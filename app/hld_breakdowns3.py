"""HLD Case Studies: Google Docs, Distributed Cache, YouTube, Job Scheduler, Web Crawler, Ad Click Aggregator, Payment System, Metrics Monitoring, ChatGPT."""
from __future__ import annotations
from .lld_practice import quiz, Lesson

def google_docs_lesson() -> Lesson:
    return Lesson(id="hld-google-docs", number=220, title="Google Docs (Collaborative Editing)",
        focus=("OT vs CRDT","operational transformation","websockets","document sharding","version vector"),
        overview="Design a highly concurrent, real-time collaborative text editor like Google Docs.",
        patterns=(), badge="HLD Case Study", tasks=(
        quiz("gdoc-1","Which algorithmic approach is most commonly used in Google Docs to resolve concurrent editing conflicts?","medium",("case-study",),
             ("Operational Transformation (OT), where the server transforms incoming operations relative to concurrent ones", "Pessimistic locking (SELECT FOR UPDATE) that locks the paragraph whenever a user starts typing", "Conflict-Free Replicated Data Types (CRDTs), which assign unique global identifiers to every single character", "Eventual consistency where the latest document snapshot completely overwrites previous edits"), 0,
             "Google Docs uses OT, which transforms editing operations (insert/delete) against concurrent operations to ensure consistency."),
        quiz("gdoc-2","What is a major conceptual difference between Operational Transformation (OT) and CRDTs?","hard",("case-study",),
             ("OT does not support offline editing states at all", "OT is only designed for desktop browsers; CRDTs are used exclusively on mobile devices", "OT requires a centralized server to order and transform operations; CRDTs are peer-to-peer and mathematically resolve conflicts state-wise", "OT requires twice as much database storage capacity as CRDTs"), 2,
             "OT relies on a single central server to act as the single source of truth; CRDTs can merge edits in any order without a coordinator."),
        quiz("gdoc-3","How does Google Docs push document updates real-time to all active collaborators?","medium",("case-study",),
             ("Sending daily email digests summarizing edit histories to all registered users", "Establishing persistent WebSockets connections between collaborators and the document coordination server", "Writing all edits directly to a global relational database write replica", "Forcing the client browser to submit high-frequency HTTP short-polling requests every 100 milliseconds"), 1,
             "WebSockets allow fast, bi-directional, low-latency communication to push and pull character changes instantly."),
        quiz("gdoc-4","How does the system represent the document in the server's working memory for active edits?","hard",("case-study",),
             ("An encrypted SQLite database loaded in RAM for every single active user session", "A series of static HTML files distributed via CDNs globally", "An in-memory data structure (like a rope or tree) representing the document text, associated with active user sessions", "A single massive flat text file saved to a shared network drive"), 2,
             "Active documents are held in memory (e.g. as a rope structure) on dedicated document servers to transform edits with sub-millisecond lag."),
        quiz("gdoc-5","What is the role of the 'Document Server' in Google Docs?","medium",("case-study",),
             ("To execute full-text search indexing over all public documents", "To host static CSS and JS bundle assets for client-side rendering", "To manage payment subscriptions and user billing details", "To coordinate collaboration: ordering operations, applying OT, updating the master document in memory, and broadcasting edits"), 3,
             "Document servers maintain active document states, processing incoming client changes sequentially to apply transformations."),
        quiz("gdoc-6","How does Google Docs handle sharding/partitioning active documents to scale horizontally?","hard",("case-study",),
             ("Store all active documents in a single central master database with no sharding", "Shard active documents alphabetically by the author's first name", "Shard active documents by Document ID, routing all collaborators on a specific document to the exact same server instance", "Shard document pages randomly across all available application nodes"), 2,
             "Routing collaborators of a single document to the same server node simplifies synchronization, making OT ordering deterministic."),
        quiz("gdoc-7","How is a user's local cursor position represented and synchronized to other collaborators?","medium",("case-study",),
             ("The client broadcasts relative character index coordinates (or sibling node IDs) via WebSockets periodically", "Cursor updates are stored in client cookies and loaded via background SQL reads", "The client watch uploads raw GPS coordinate streams every 3 seconds", "The user's local smartphone OS reboots to flush cursor coordinates"), 0,
             "Cursors are lightweight. Synchronizing them as relative offsets or sibling IDs keeps them accurate even as text shifts."),
        quiz("gdoc-8","What mechanism does the client use to handle offline edits and merge them back when connection is restored?","hard",("case-study",),
             ("The client overwrites the server's version with the local offline snapshot directly", "The client sends the offline changes as a single massive SQL INSERT script", "The client stores offline operations in a queue, and on reconnect, plays them back while transforming against missed edits using OT", "The client deletes all offline edits and reloads the server's version"), 2,
             "On reconnect, the client sends its queued operations, and the server transforms them relative to edits that occurred during offline status."),
        quiz("gdoc-9","Why is a relational database not ideal for storing real-time, character-by-character edit histories?","medium",("case-study",),
             ("It requires using symmetric encryption keys for every single database column", "Relational databases do not support index columns for text values", "Relational databases cannot handle text documents containing special characters", "Writing every single keystroke as a separate ACID transaction causes massive write overhead and database lock degradation"), 3,
             "Keystrokes are too high-frequency. Better to batch edits on the client, or write to an append-only log / NoSQL timeseries database."),
       quiz("gdoc-10","How are historical versions (snapshots) of documents created without storing every keystroke forever?","medium",("case-study",),
             ("By forcing the user's browser to compile snapshot images locally", "By taking a full document backup every 5 seconds globally", "By deleting edit history altogether after 24 hours of inactivity", "By regularly snapshotting the document state (e.g., every 100 edits) and storing incremental change logs between snapshots"), 3,
             "Snapshotting plus delta tracking is extremely storage efficient, allowing historical restoration without storing billions of pings."),
        quiz("gdoc-11","What is a major trade-off of Operational Transformation (OT) compared to CRDTs?","hard",("case-study",),
             ("OT only works on native iOS applications", "OT is highly complex to implement correctly (concurrency edge cases) and requires a centralized server to coordinate updates", "OT does not support formatting operations like bold or italics", "OT requires 100x more memory on the client device than CRDTs"), 1,
             "OT requires highly complex transformation logic for every permutation of concurrent operations and relies on a central server."),
        quiz("gdoc-12","What type of index optimizes looking up all documents owned by a user?","easy",("case-study",),
             ("A single index on the document content text string", "A hash index covering the user's password", "A composite index on (owner_id, created_at DESC)", "A partial index built only on documents containing comments"), 2,
             "A composite index on (owner_id, created_at DESC) allows the dashboard to instantly load the user's files sorted by date."),
        quiz("gdoc-13","What is the role of a 'Presence Service' in collaborative editing platforms?","medium",("case-study",),
             ("To run full-text search indexing on active documents", "To manage billing subscriptions and account upgrades", "To track active connections and show who is currently viewing or editing the document", "To secure transactional checkout operations"), 2,
             "The presence service maps active users to the document, maintaining a real-time list of concurrent collaborators."),
        quiz("gdoc-14","How does Google Docs handle edit conflicts if two users type at the exact same location concurrently?","hard",("case-study",),
             ("The server overwrites the first letter with the second letter", "The document splits into two separate files automatically", "OT adjusts the character offset of the second operation by +1, so both letters are kept in sequence instead of overwriting", "The server rejects the second user's keystroke, throwing an error"), 2,
             "OT transforms offsets. E.g., if User A inserts 'x' at index 5 and User B inserts 'y' at index 5, B's edit is shifted to index 6."),
        quiz("gdoc-15","What is the benefit of a Document Cache (like Redis) in this system?","medium",("case-study",),
             ("To perform real-time video transcodings during active editing sessions", "To store raw document metadata and active session state in RAM for fast client dashboard loading", "To completely replace the persistent storage layer for documents", "To execute code sandboxing checks on uploaded text strings"), 1,
             "Caching active document metadata and session counts in memory ensures fast rendering of dashboard timelines and collaboration invites."),
        quiz("gdoc-16","Why is a composite index on (document_id, version) useful in the revisions database table?","hard",("case-study",),
             ("It optimizes fetching specific historical change segments (deltas) chronologically for a target document", "It encrypts the revision history rows automatically", "It replaces the need for keeping document snapshots", "It is required to run background full-text searches"), 0,
             "This composite index allows the revision service to retrieve the sequence of operations needed to build a specific document version."),
        quiz("gdoc-17","What is the advantage of using WebSockets instead of Server-Sent Events (SSE) for collaborative editing?","medium",("case-study",),
             ("WebSockets automatically encrypt all data payloads by default", "SSE only works on mobile devices; WebSockets run on all desktop browsers", "WebSockets are bidirectional, allowing both sending edits and receiving updates on a single low-overhead connection", "SSE requires using custom network cards; WebSockets do not"), 2,
             "SSE is unidirectional (server-to-client). WebSockets allow bidirectional streaming, reducing overhead for high-frequency editing inputs."),
        quiz("gdoc-18","How are document comments and suggestions stored relative to the main document text?","hard",("case-study",),
             ("Embedded directly as plain text inside the raw document content string", "Stored in separate relational tables linked by document ID, anchored to specific stable character range identifiers or DOM nodes", "Saved as metadata inside the client's local cookies payload", "Logged in flat files on a shared network mounting drive"), 1,
             "Comments live in secondary tables, utilizing stable character anchors (or node IDs) so edits don't misalign comment highlights."),
        quiz("gdoc-19","What is the role of the 'Snapshot Janitor Service' in this architecture?","medium",("case-study",),
             ("To delete documents that have not been modified for 30 days", "To sweep revision histories, merging micro-changes into periodic flat snapshots to optimize storage and loading speeds", "To run full-text search indexing routines on cold documents", "To manage user session authentication tokens"), 1,
             "Compacting change history into major snapshots prevents the load time of old documents from lagging due to executing millions of operations."),
        quiz("gdoc-20","What type of locking is most appropriate when updating document permissions (e.g. sharing with a friend)?","medium",("case-study",),
             ("Pessimistic locking (SELECT FOR UPDATE) to block all reads during the change", "Distributed Redlock across all global app servers", "A database-wide table lock on the documents table", "Optimistic locking (version numbers) since sharing settings change rarely and have very low contention"), 3,
             "Permissions are rarely updated concurrently. Optimistic locking guarantees integrity with negligible performance cost."),
    ))

def distributed_cache_lesson() -> Lesson:
    return Lesson(id="hld-dist-cache", number=221, title="Distributed Cache",
        focus=("cache eviction","consistent hashing","thundering herd","cache stampede","redis cluster"),
        overview="Design a highly available, low-latency distributed caching system like Redis or Memcached.",
        patterns=(), badge="HLD Case Study", tasks=(
        quiz("dch-1","What is the primary benefit of consistent hashing in a distributed cache cluster?","medium",("case-study",),
             ("It automatically balances CPU usage across all application servers", "It ensures that adding or removing a cache node only invalidates a minimal fraction (1/N) of keys", "It guarantees that all cache keys are encrypted using symmetric algorithms", "It routes all write operations to a single centralized database instance"), 1,
             "Consistent hashing maps keys to a ring, minimizing cache misses when nodes scale up or down compared to simple mod hashing (hash(key) % N)."),
        quiz("dch-2","What is a cache stampede (thundering herd), and how is it mitigated?","hard",("case-study",),
             ("When client devices reboot repeatedly, wiping local session caches", "When replication lag causes read servers to see contradictory data structures", "When multiple cache nodes attempt to write to the same disk sector concurrently", "When a popular key expires, and concurrent requests hit the database simultaneously, overwhelming it; solved via locking/single-flight"), 3,
             "A stampede occurs when a hot cache key expires. Mutexes (or 'single-flight' patterns) ensure only one worker queries the DB to refresh it."),
        quiz("dch-3","What is the difference between Write-Through and Write-Back (Write-Behind) caching strategies?","medium",("case-study",),
             ("Write-Through is designed for SSDs; Write-Back is designed for RAM disks", "Write-Through requires using symmetric keys; Write-Back uses public key pairs", "Write-Through writes to cache and DB synchronously; Write-Back writes to cache immediately and async to DB in background", "Write-Through is eventually consistent; Write-Back is strongly consistent"), 2,
             "Write-Through is safer but slower. Write-Back is extremely fast for writes but risks data loss if the cache node crashes before DB write."),
        quiz("dch-4","Which cache eviction policy evicts the key that has not been accessed for the longest duration?","easy",("case-study",),
             ("Random Replacement (RR)", "First-In-First-Out (FIFO)", "Least Frequently Used (LFU)", "Least Recently Used (LRU)"), 3,
             "LRU tracks access order, evicting the least recently used element when the memory limit is reached."),
        quiz("dch-5","How does the LFU eviction policy differ from LRU?","medium",("case-study",),
             ("LFU requires double the memory capacity of LRU", "LFU is only designed for relational databases", "LFU evicts keys randomly to preserve memory structures", "LFU evicts keys based on raw access count; LRU evicts based on the elapsed time since the last access"), 3,
             "LFU keeps popular keys even if unaccessed recently, but needs frequency counters which can bloat memory if not optimized (e.g. TinyLFU)."),
        quiz("dch-6","Why is Redis single-threaded, and how does it achieve extreme performance?","hard",("case-study",),
             ("It runs exclusively on single-core custom hardware processors", "It disables all networking protocol layers to communicate directly with OS buffers", "It forces all connected clients to execute operations locally in JavaScript", "It uses a non-blocking I/O multiplexed event loop (epoll) to handle thousands of requests in memory without context switching"), 3,
             "Redis runs in-memory and handles network pings via an event multiplexer, completely avoiding thread locks and context switching overhead."),
        quiz("dch-7","What is the role of virtual nodes in consistent hashing?","hard",("case-study",),
             ("To encrypt cache queries during transit", "To coordinate distributed locks across different datacenters", "To ensure a highly uniform distribution of keys across physical cache servers, preventing hot/skewed nodes", "To split cache keys into binary representations for storage"), 2,
             "Physical nodes are mapped to multiple virtual positions on the hashing ring, smoothing out geographic imbalances in key distribution."),
        quiz("dch-8","How does Redis Cluster handle partition tolerance and data sharding?","hard",("case-study",),
             ("It shards keys across 16,384 logical hash slots, distributing slots among active nodes with master-replica failovers", "It stores all data in a single massive relational database cluster", "It requires using external ZooKeeper coordinators to assign slots", "It duplicates all cache keys across every physical node in the network"), 0,
             "Redis Cluster divides slots dynamically. Clients route to nodes containing the slot, and replicas handle master failures autonomously."),
        quiz("dch-9","What is a major trade-off of the Cache-Aside pattern?","medium",("case-study",),
             ("It automatically deletes historical logs after 24 hours", "It is only compatible with PostgreSQL databases", "It can lead to stale data if the database is updated and the cache key is not invalidated, and incurs cache miss latency on first read", "It requires using symmetric encryption keys for every query"), 2,
             "Cache-Aside places caching logic on the client/application. It can read stale data if updates fail to clear the cache key."),
        quiz("dch-10","What technology is best for handling master failover in a non-clustered Redis setup?","medium",("case-study",),
             ("A distributed consensus group hosted inside a relational database", "Redis Sentinel, which monitors master-replica nodes, performs health checks, and automates failovers", "Restoring the cache database from an S3 backup file automatically", "A local shell script running on client devices"), 1,
             "Redis Sentinel provides high availability: monitoring, notifying, and coordinating automatic failover to replicas when master dies."),
        quiz("dch-11","What is the difference between Cache Penetration and Cache Avalanche?","hard",("case-study",),
             ("Penetration is key storage in RAM; Avalanche is write backup degradation", "There is no difference; they are different terms for the same concept", "Penetration is CDN caching failure; Avalanche is database shard crash", "Penetration is querying non-existent keys (hitting DB every time); Avalanche is many keys expiring at once (overwhelming DB)"), 3,
             "Penetration is solved via Bloom filters or caching nulls. Avalanche is solved via randomizing TTL expirations (jitter)."),
        quiz("dch-12","How does a Bloom Filter prevent Cache Penetration?","hard",("case-study",),
             ("It checks if a key definitely does not exist in the database using a fast, space-efficient bit array, avoiding empty DB queries", "It automatically encrypts the non-existent key strings", "It routes the request to an alternative cloud database", "It stores the actual key values in compressed binary format"), 0,
             "Bloom filters have zero false negatives: if it says a key doesn't exist, it definitely doesn't, allowing us to block useless DB reads."),
        quiz("dch-13","What is the 'active replication' pattern in cache synchronization?","hard",("case-study",),
             ("Running a background sweeping thread that synchronizes cache keys every 10 milliseconds", "A push system that sends all cache modifications to client devices", "Updating cache keys exclusively using database triggers", "Having all application writes update both the primary and replica caches in parallel"), 3,
             "Active replication coordinates simultaneous updates across nodes, keeping caches in sync at the cost of write complexity."),
        quiz("dch-14","What is the role of the replication backlog buffer in Redis replica synchronization?","hard",("case-study",),
             ("To queue client WebSocket connections", "To cache historical query execution plans", "To store raw database backups on local SSDs", "To store write commands executed while a replica was disconnected, enabling fast partial resync on reconnect"), 3,
             "The backlog is a circular buffer. If a replica disconnects briefly, it can catch up via backlog deltas instead of running a full sync."),
        quiz("dch-15","What is the benefit of a multi-level cache (L1 local RAM + L2 distributed Redis)?","medium",("case-study",),
             ("L1 local cache provides near-zero latency (<1 microsecond) for hot keys, while L2 handles shared network-wide cache states", "It automatically translates cache values into JSON structures", "It eliminates the need for any relational database backups", "It operates without consuming server CPU cycles"), 0,
             "Multi-level caching balances speed: L1 avoids network calls entirely; L2 provides a shared, highly available backing store."),
        quiz("dch-16","What index optimizes looking up cache keys in an in-memory hash table?","easy",("case-study",),
             ("A B-tree index on the key name", "A clustered index sorting all keys alphabetically", "A partial index built only on text keys", "A hash index (providing O(1) average lookup time)"), 3,
             "In-memory caches utilize hash tables where keys map directly to memory addresses, guaranteeing O(1) access times."),
        quiz("dch-17","What is the primary drawback of using the Write-Back caching strategy?","medium",("case-study",),
             ("It is computationally too slow to run on modern servers", "It forces all reads to execute full table database scans", "It is only compatible with SQLite storage engines", "If the caching node crashes before the background worker flushes edits, updates are lost forever"), 3,
             "Write-Back prioritizes speed over safety, creating a window of vulnerability where data exists only in volatile memory."),
        quiz("dch-18","How does Memcached handle memory allocation, and how does it prevent memory fragmentation?","hard",("case-study",),
             ("By compressing all value payloads to 1 byte", "By running active background defragmentation sweeps every 5 seconds", "By storing all cache records in dynamic heap arrays", "By using a Slab Allocator, dividing memory into pre-allocated slabs of specific sizes (chunk sizes) to avoid malloc fragmentations"), 3,
             "Slab allocation manages RAM efficiently, placing cache items into closely matched chunk sizes to avoid Swiss-cheesing the memory."),
        quiz("dch-19","What is the maximum key and value size limit in standard Memcached?","hard",("case-study",),
             ("Key: 1 megabyte, Value: 1 gigabyte", "Key: 10 kilobytes, Value: 50 megabytes", "Key: 250 bytes, Value: 1 megabyte", "No limit; size is bound only by server RAM capacity"), 2,
             "Memcached enforces a strict limit: keys cannot exceed 250 bytes, and individual value payloads are capped at 1MB."),
        quiz("dch-20","What type of cache eviction policy is Adaptive Replacement Cache (ARC), and what is its advantage over LRU?","hard",("case-study",),
             ("It automatically evicts keys that contain query arguments", "It evicts keys based on their storage size in bytes", "It is a randomized algorithm requiring zero memory tracker lists", "It dynamically tunes between recency (LRU) and frequency (LFU) based on cache hit feedback, outperforming both"), 3,
             "ARC maintains double tracking lists (for recency and frequency), self-adjusting to changing application access patterns dynamically."),
    ))

def youtube_lesson() -> Lesson:
    return Lesson(id="hld-youtube", number=222, title="YouTube (Video Streaming)",
        focus=("video chunking","transcoding","HLS/DASH","CDN delivery","haystack storage"),
        overview="Design a highly scalable global video upload, processing, and streaming platform like YouTube.",
        patterns=(), badge="HLD Case Study", tasks=(
        quiz("yt-1","What is the first step in processing an uploaded raw high-resolution video file?","medium",("case-study",),
             ("Encrypt the video using asymmetric key rings before saving it to disk", "Segment/chunk the video into small, 2-10 second temporal chunks for parallel processing and adaptive streaming", "Index every frame in an inverted database for text search", "Broadcast the raw file directly via WebSockets to all subscribers"), 1,
             "Chunking is crucial: it allows parallel transcoding, fast uploading (resuming failures), and dynamic delivery formats."),
        quiz("yt-2","What is video transcoding, and why is it necessary for a streaming service?","medium",("case-study",),
             ("Translating video subtitles into multiple languages dynamically", "Obfuscating video filenames using random string transformations", "Scanning the audio track to detect copyright violations using hashing", "Compressing and converting videos into multiple formats (e.g. MP4, WebM) and resolutions (1080p, 780p, 360p) to match user bandwidth"), 3,
             "Transcoding generates optimized streams for different screen sizes and connection speeds, preventing stuttering."),
        quiz("yt-3","How do modern players implement 'Adaptive Bitrate Streaming' (e.g. HLS or DASH)?","hard",("case-study",),
             ("The client browser reboots when network speed fluctuates", "The database triggers alternative writes to disk arrays", "The client player monitors local network speeds and dynamically requests the next video chunk in a higher or lower bitrate", "The server adjusts its CPU output dynamically to throttle the client's network connection"), 2,
             "HLS (Apple) and DASH (standard) split video into chunks at varying bitrates. The client player downloads the best fit dynamically."),
        quiz("yt-4","Which storage pattern is optimized for storing billions of raw, static, write-once video files?","medium",("case-study",),
             ("A distributed graph database containing all video metadata", "An in-memory Redis cache with high-frequency snapshots", "A strongly consistent relational database using large binary object (BLOB) columns", "An object storage system (like AWS S3 or Google Cloud Storage) behind a global CDN network"), 3,
             "Object stores scale storage capacity independently and cheaply, serving as the immutable backing store for CDN origin pulls."),
        quiz("yt-5","What is the role of a CDN (Content Delivery Network) in video streaming?","easy",("case-study",),
             ("To cache and serve video chunks from edge servers geographically close to the user, reducing latency and origin server bandwidth", "To index user profile comments for fast search lookups", "To transcode raw videos into different device formats", "To encrypt user password credentials before saving them"), 0,
             "CDNs distribute the massive bandwidth load of video streaming, bypassing origin servers for 99% of global playback requests."),
        quiz("yt-6","How does the system handle high-concurrency metadata lookups (e.g. video details, view counts)?","medium",("case-study",),
             ("Cache video metadata in Redis using the Cache-Aside pattern (key is video_id), updating view counters asynchronously", "Write metadata details to client browser local storage cookies", "Force client devices to download the metadata database locally", "Query the primary relational database write replica on every view click"), 0,
             "Video metadata is read-heavy and static. Caching in Redis ensures sub-millisecond lookups under massive viral traffic spikes."),
        quiz("yt-7","What is the purpose of a 'Manifest File' (.m3u8 or .mpd) in video streaming?","hard",("case-study",),
             ("It contains the user's payment details and billing histories", "It runs deep learning algorithms to recommend the next video", "It lists the URLs of all video chunks, their resolutions, and bitrates, telling the client player how to request chunks", "It encrypts the audio tracks for copyright protection"), 2,
             "The manifest acts as the playlist/manifest index. The player reads it first to understand how to fetch and stitch chunks together."),
        quiz("yt-8","How should the system scale video uploads to prevent network failures from wiping progress?","hard",("case-study",),
             ("Force client browsers to compress the video locally to 1MB first", "Force clients to upload the entire 10GB video in a single HTTP POST request", "Block all uploads that occur during peak traffic hours", "Use multi-part chunked uploads with a stateful upload manager, allowing the client to resume from the last successful chunk ID"), 3,
             "Resumable multi-part upload splits the file. If connection drops, the client query finds the next missing chunk, avoiding full restarts."),
        quiz("yt-9","What is a major challenge of storing millions of tiny video thumbnails, and how is it solved?","hard",("case-study",),
             ("Thumbnails must be encrypted using distributed public key rings", "Relational databases cannot index image columns", "Headless browsers must render thumbnails dynamically on every hover", "Small files waste disk inodes and incur high disk seek latency; solved by packing thumbnails into a single image sprite/atlas"), 3,
             "Like Facebook's Haystack, stitching small images into a large file (and indexing offsets in memory) avoids heavy OS disk-seek overhead."),
        quiz("yt-10","Why is an asynchronous task pipeline (e.g., Celery or temporal) required for video uploads?","medium",("case-study",),
             ("To verify the user's credit card details during active playback", "To transcode video, generate thumbnails, check copyright (fingerprinting), and update search indexes off the upload confirmation path", "To delete historical user video uploads after 30 days of inactivity", "To route WebSocket chat connections to the gateway servers"), 1,
             "Uploading is fast. Transcoding and scanning are slow. Decoupling them keeps the upload snappy while background workers do the heavy lifting."),
        quiz("yt-11","What is Video Fingerprinting, and how is it used?","medium",("case-study",),
             ("Using cryptographic hash algorithms (like MD5) to verify video filenames", "Obfuscating the video frames using random vector transforms", "Analyzing the physical touch inputs of users on the smartphone screen", "Generating a unique structural hash of the audio/video stream to match and flag copyrighted material against a database"), 3,
             "Copyright systems (like ContentID) compare structural audio-visual fingerprints to identify copyrighted uploads dynamically."),
        quiz("yt-12","What type of index optimizes looking up all videos uploaded by a user sorted by upload date?","easy",("case-study",),
             ("A hash index built on the video title column", "A partial index built only on videos that contain ads", "A single index on the video description text string", "A composite index on (user_id, uploaded_at DESC)"), 3,
             "Composite index on (user_id, uploaded_at DESC) allows the user's channel dashboard to load uploaded videos instantly pre-sorted."),
        quiz("yt-13","How does the system optimize caching of video chunks on CDNs?","hard",("case-study",),
             ("Cache only the first 10-30 seconds of popular videos (cold-start optimization) at the edge, fetching the rest on-demand", "Cache every single video chunk globally at all CDN edge nodes permanently", "Disable CDN caching entirely and stream directly from primary databases", "Force client devices to share cached chunks locally via Bluetooth networks"), 0,
             "Caching entire catalogs is impossible. Caching the start of popular videos guarantees instant playback, loading remainder dynamically."),
        quiz("yt-14","What type of sharding optimizes the video metadata database to scale horizontally?","hard",("case-study",),
             ("Shard dynamically based on the length of the video file in bytes", "Store all video records in a single partition hosted on an active SSD array", "Shard data by Video ID to ensure uniform write and read distribution across shards", "Shard alphabetically by the video's description text"), 2,
             "Sharding by Video ID ensures uniform load, preventing hot shards (e.g., if we sharded by creator, celebrity creators would break nodes)."),
        quiz("yt-15","What is the role of a Video Packaging Service?","hard",("case-study",),
             ("To balance HTTP connections across frontend gateways", "To assemble transcoded video chunks into specific protocol streams (HLS/DASH) and apply DRM encryption on-the-fly", "To print physical mailing labels for video shipments", "To encrypt video files before they are written to S3 disks"), 1,
             "Packaging takes raw transcoded segments and bundles them with appropriate manifest indexes and DRM keys during distribution."),
        quiz("yt-16","What index optimizes looking up a video's details in a NoSQL Document Store?","easy",("case-study",),
             ("A composite index on (video_id, title)", "A partial index on active view count", "A single primary key index on video_id", "A hash index on the uploader's email"), 2,
             "Querying details is a simple key-value lookup. A primary key index on video_id resolves in O(1) time in document tables."),
        quiz("yt-17","What is the primary trade-off of high-performance video compression codecs (like AV1 or HEVC)?","hard",("case-study",),
             ("They are incompatible with standard cloud virtual machine servers", "They can only render in black-and-white formats on mobile devices", "They completely disable adaptive bitrate streaming features", "They reduce video file sizes by 30-50% (saving bandwidth) but require significantly higher server-side transcoding CPU/compute"), 3,
             "Modern codecs save massive network delivery costs but demand severe CPU processing, making transcoding slower and costlier."),
        quiz("yt-18","How does YouTube handle real-time playback logs for analytics feeds?","medium",("case-study",),
             ("Write logs directly to relational database tables on every single frame played", "Buffer playback logs on the client device, flush in batches to Kafka, and process via streaming analytics (Flink/Spark)", "Reboot the application servers every time network utilization spikes", "Store all playback traces in the client browser's local cookies"), 1,
             "Streaming pipelines handle high-throughput telemetry data, enabling dashboards to track live view counts safely off the main read path."),
        quiz("yt-19","What type of locking is most appropriate when updating a video's status from 'Processing' to 'Active'?","medium",("case-study",),
             ("Enforcing a database-wide table lock on the videos table", "Distributed Redlock across all active global application servers", "Optimistic locking (version numbers) since status updates occur exactly once at the end of the transcoding pipeline", "Pessimistic locking (SELECT FOR UPDATE) to block all reads during the update phase"), 2,
             "Since there is no contention on status transitions (only the transcode worker writes it), optimistic locking is cheap and ideal."),
        quiz("yt-20","What index optimizes looking up all video comments sorted by likes?","hard",("case-study",),
             ("A composite index on (video_id, like_count DESC)", "A clustered index sorting all comments globally by creation date", "A hash index built on the commenter's profile avatar URL", "A single index on the comment text string"), 0,
             "A composite index on (video_id, like_count DESC) allows the comment section to render top comments instantly pre-sorted."),
    ))

def job_scheduler_lesson() -> Lesson:
    return Lesson(id="hld-job-scheduler", number=223, title="Distributed Job Scheduler",
        focus=("cron triggers","at-least-once","distributed locks","leader election","dead-letter queue"),
        overview="Design a resilient, highly available distributed job scheduling and execution platform.",
        patterns=(), badge="HLD Case Study", tasks=(
        quiz("jsch-1","What is the primary objective of a Distributed Job Scheduler compared to a single-node Cron?","medium",("case-study",),
             ("To execute database backups dynamically on every CPU cycle", "To speed up code execution using client-side JavaScript worker threads", "To automatically encrypt the task scripts inside the database", "To guarantee high availability and coordinate execution across a cluster, avoiding duplicate executions"), 3,
             "Distributed schedulers coordinate tasks to ensure they run exactly when scheduled, and survive node crashes without double running."),
        quiz("jsch-2","How does the system ensure that a scheduled task is only executed by one worker node?","hard",("case-study",),
             ("By asking users to manually select which worker should execute the task", "By storing task completion reports in client browser cookies", "By broadcasting the execution script to all workers and letting them compete", "By acquiring a distributed lock (e.g., using Redis Redlock or ZooKeeper) or using atomic database transaction status updates"), 3,
             "Acquiring an atomic execution lock or marking a database task record as 'IN_PROGRESS' using SELECT FOR UPDATE prevents double-execution."),
        quiz("jsch-3","What is the difference between 'at-least-once' and 'at-most-once' execution guarantees?","hard",("case-study",),
             ("At-least-once is for SSD arrays; at-most-once is for RAM cache", "At-least-once is strongly consistent; at-most-once is eventually consistent", "At-least-once requires symmetric keys; at-most-once uses asymmetric keys", "At-least-once guarantees the task runs but can duplicate on failures; at-most-once guarantees no duplicates but can skip runs"), 3,
             "At-least-once requires acknowledgment flows and retries (risking duplicates). At-most-once locks early and never retries on failure (risking drops)."),
        quiz("jsch-4","What is the role of a Dead-Letter Queue (DLQ) in a job scheduler?","medium",("case-study",),
             ("To route WebSocket client connections to the gateway", "To cache historical query execution logs", "To capture and store failed job events that exceeded maximum retry limits for isolation and manual auditing", "To store raw database backups on local SSDs"), 2,
             "A DLQ keeps toxic/un-processable jobs from repeating forever, isolating them so developers can debug why they failed."),
        quiz("jsch-5","How is a scheduler's active leader elected in a highly available setup?","hard",("case-study",),
             ("By choosing a leader randomly on every incoming request", "By allowing the worker with the highest CPU count to self-promote", "By using a consensus algorithm (Raft or Paxos) via coordination services like ZooKeeper or Consul to maintain a leader lease", "By asking the relational database to run background alphabetical sorting"), 2,
             "Leader election ensures a single primary coordinator manages the queue and triggers tasks, while replicas stand ready to take over."),
        quiz("jsch-6","Why is database polling inefficient for a high-volume, sub-second job scheduler?","hard",("case-study",),
             ("Polling creates continuous, heavy read queries on the database, degrading performance; better to use time-sorted in-memory heaps", "Relational databases do not support date range query filters", "Polling forces the client browser to refresh all CSS styling sheets", "Polling automatically deletes historical log tables"), 0,
             "Repeatedly running `SELECT ... WHERE trigger_time <= NOW()` wastes DB cycles. Better to load near-future tasks to memory structures."),
        quiz("jsch-7","Which data structure is ideal for tracking tasks sorted by their scheduled execution time in memory?","medium",("case-study",),
             ("A hash index built on the task's script name", "A flat text log file stored on a shared network drive", "A Min-Heap or Redis Sorted Set (ZSET) where the score is the epoch execution timestamp", "A double-ended queue with random insertion capabilities"), 2,
             "Min-Heaps and ZSETs keep elements sorted, letting the scheduling thread fetch the next due task in O(log N) or O(1) time."),
        quiz("jsch-8","How do you handle 'thundering herd' spikes when thousands of scheduled jobs trigger at the exact same second?","hard",("case-study",),
             ("Directly increase database write locks to block non-contest traffic", "Force the client browser to reboot when traffic utilization spikes", "Block all incoming jobs that occur during peak morning hours", "Distribute jobs across multiple message broker partitions and use worker pools with active rate-limit queues"), 3,
             "Worker thread pooling and message queues absorb the trigger spike, processing jobs smoothly according to worker capacities."),
        quiz("jsch-9","What is the advantage of using a DAG (Directed Acyclic Graph) in a job scheduler?","medium",("case-study",),
             ("It runs task scripts inside client web browsers locally", "It models and coordinates complex job dependencies, ensuring child tasks run only after parent tasks complete successfully", "It replaces the need for keeping relational database tables", "It automatically compresses the task execution binary size by 90%"), 1,
             "DAGs represent workflows (e.g. Data Ingestion -> Transcoding -> Report generation), executing parallel paths safely."),
        quiz("jsch-10","How should the system monitor whether a worker node died mid-execution of a critical job?","hard",("case-study",),
             ("Force the database to lock the user's entire account profile row", "Ask other workers to ping the target node via Bluetooth periodically", "Let the client device query the worker status every 2 seconds", "Workers send periodic heartbeats while executing; if heartbeats cease, the scheduler coordinator re-queues the job"), 3,
             "Heartbeat leases guarantee resilience. If a node goes silent, its lease expires, and the coordinator handles the rollback/retry."),
        quiz("jsch-11","What is the purpose of an 'Idempotent Task Design' in a distributed scheduler?","hard",("case-study",),
             ("To ensure that running a task multiple times (e.g., due to retries) results in the exact same system state as running it once", "To verify the physical location of the worker's IP address", "To encrypt task payloads during transport layer security (TLS) handshakes", "To index task columns inside the relational database"), 0,
             "Since network failures can trigger duplicate executions (at-least-once), tasks must be idempotent to avoid double payments/writes."),
        quiz("jsch-12","What type of database is highly optimized for storing historical execution audits and task metrics?","medium",("case-study",),
             ("An in-memory Redis cache with high-frequency snapshotting", "A graph database specialized in relationship mapping", "An OLAP column-store database (like ClickHouse or Snowflake) for massive aggregations and analytics", "A local SQLite instance stored on the client device"), 2,
             "OLAP databases handle millions of historical logs, allowing developers to query execution histories and failure trends fast."),
        quiz("jsch-13","What is the benefit of a dynamic worker pooling model?","medium",("case-study",),
             ("It automatically translates task scripts into multiple languages", "It eliminates the need for maintaining relational database backups", "It scales the worker fleet up during heavy job backlogs and down during idle times, optimizing cloud compute costs", "It operates without consuming server CPU cycles"), 2,
             "Dynamic auto-scaling monitors queue length, spawning new VM/container instances when jobs back up, preserving performance."),
        quiz("jsch-14","What type of index optimizes looking up all scheduled tasks within a specific time window?","easy",("case-study",),
             ("A hash index built on the owner's user ID", "A partial index built only on tasks that failed", "A single index on the task script name", "A composite index on (status, scheduled_time)"), 3,
             "A composite index on (status, scheduled_time) allows the engine to fetch all pending or ready-to-run tasks instantly."),
        quiz("jsch-15","What is a major limitation of using a shared filesystem (like NFS) to coordinate job status?","hard",("case-study",),
             ("File locking in shared networks is notoriously slow, unreliable, and susceptible to split-brain states", "NFS automatic backups wipe active job logs every hour", "Shared drives require custom hardware cards on every worker chassis", "NFS drives cannot store files containing JSON configurations"), 0,
             "Shared disk file locking has major latency and reliability issues. Use specialized databases or consensus locks instead."),
        quiz("jsch-16","How are ad-hoc delay tasks (e.g., 'run task X exactly 4 hours from now') handled elegantly at scale?","hard",("case-study",),
             ("Create a separate Postgres database shard for every ad-hoc request", "Store them in a delayed message queue (like RabbitMQ delayed exchange or Kafka with timestamp index) or Redis ZSET", "Require the user's browser to stay open for 4 hours to trigger the call", "Force the application server to call time.sleep() for 4 hours"), 1,
             "Delayed queues or Redis ZSETs store the task, popping it off only when the target epoch time has been crossed."),
        quiz("jsch-17","What is the primary trade-off of a short heartbeat interval (e.g. 500ms) for worker check-ins?","hard",("case-study",),
             ("It works only on mobile device frameworks, not desktop web browsers", "It detects failures extremely fast but creates massive network traffic and CPU overhead on the coordination server", "It requires using symmetric encryption keys for every single update summary", "It prevents workers from executing multi-step task calculations"), 1,
             "Frequent heartbeats are chatty and can overwhelm the leader node. A balance (e.g., 5-10 seconds) is usually preferred."),
        quiz("jsch-18","How does the scheduler prevent a single buggy, long-running job from locking up a worker node indefinitely?","medium",("case-study",),
             ("Block the user's account if their task runs for more than 1 minute", "Allow all tasks to run forever to preserve partial results", "Enforce strict execution timeouts (TTL) on tasks, terminating the worker thread or container if the limit is exceeded", "Reboot the entire active application server cluster automatically"), 2,
             "Task timeouts protect worker availability, killing runaway processes to ensure the worker can accept next queued jobs."),
        quiz("jsch-19","What is the role of the 'Coordinator Node' in a decentralized job scheduler?","medium",("case-study",),
             ("To encrypt user password credentials", "To parse user script syntax for indexing in search engines", "To host static UI assets for the scheduling dashboard", "To distribute tasks, track active worker nodes, balance load, and handle failovers of execution leases"), 3,
             "The coordinator acts as the master, orchestrating queue flows and assigning tasks to healthy workers dynamically."),
        quiz("jsch-20","What index optimizes looking up the execution history of a specific job_id sorted by run date?","easy",("case-study",),
             ("A single index on the job description text string", "A partial index built only on successful runs", "A composite index on (job_id, started_at DESC)", "A hash index built on the worker's machine name"), 2,
             "A composite index on (job_id, started_at DESC) allows the UI to render the historical runs of a job instantly pre-sorted."),
    ))

def web_crawler_lesson() -> Lesson:
    return Lesson(id="hld-web-crawler", number=224, title="Distributed Web Crawler",
        focus=("url frontier","politeness","DNS cache","SimHash deduplication","robots.txt"),
        overview="Design a highly scalable, polite distributed web crawler to index the entire World Wide Web.",
        patterns=(), badge="HLD Case Study", tasks=(
        quiz("wcrl-1","What is the role of the URL Frontier in a web crawler design?","medium",("case-study",),
             ("To route browser search queries to the nearest datacenter", "To manage and prioritize the queue of URLs to be crawled, ensuring politeness and priority constraints are met", "To encrypt crawled page metadata before index writing", "To store raw parsed HTML pages on local SSDs"), 1,
             "The URL Frontier is the core component. It manages which URLs to visit next, scheduling them by importance and domain limits."),
        quiz("wcrl-2","Why is a Domain-Level Queue structure necessary inside the URL Frontier?","hard",("case-study",),
             ("To enforce politeness: preventing the crawler from hammering a single website with concurrent requests, violating ethics", "To speed up the network DNS resolution of coordinate domains", "To automatically group websites by their legal hosting country", "To store crawl histories of users in localized geographical partitions"), 0,
             "Politeness dictates crawling one site at a time with a delay between requests. Domain queues separate URLs per target host."),
        quiz("wcrl-3","How does a web crawler avoid downloading duplicate web pages containing identical text content?","hard",("case-study",),
             ("By calculating a locality-sensitive hash (like SimHash or MinHash) of parsed pages, comparing them against seen hashes", "By checking if the HTML file size in bytes matches previously indexed pages", "By forcing the web server to return a unique page transaction key", "By translating the text into Base58 encoded string arrays"), 0,
             "SimHash generates similar hash signatures for highly similar text, enabling the crawler to skip minor modifications (like ads/dates)."),
        quiz("wcrl-4","What is the purpose of a DNS Cache in a high-performance distributed crawler?","medium",("case-study",),
             ("To encrypt transport layer security (TLS) handshakes", "To store static CSS and JS bundle assets", "To manage payment subscriptions and user billing details", "To avoid querying external DNS servers repeatedly, resolving hostnames to IP addresses locally in microseconds"), 3,
             "DNS lookup is a slow network step. Caching IP addresses locally prevents crawling threads from blocking on name resolution."),
        quiz("wcrl-5","How does a distributed crawler handle robots.txt files ethically?","medium",("case-study",),
             ("It requires developer sign-offs on every single target URL crawl", "It ignores robots.txt if the website uses standard HTML formatting", "It parses and caches robots.txt for every domain, strictly obeying its Disallow and Crawl-delay directives", "It overwrites the robots.txt file on the target server with permissions"), 2,
             "Obeying robots.txt is the legal and ethical standard for crawlers, preventing access to private or heavy routes."),
        quiz("wcrl-6","What type of data structure is ideal for storing millions of crawled URL signatures to check seen status fast?","hard",("case-study",),
             ("A series of static HTML files distributed via CDNs globally", "A space-efficient Bloom Filter in memory (or a distributed set in Redis), checking URL seen state in O(1) time", "An encrypted SQLite database loaded in RAM", "A single massive flat text file saved to a shared network drive"), 1,
             "Bloom filters handle massive cardinality with tiny memory footprints, instantly filtering out already visited URLs."),
        quiz("wcrl-7","How does a crawler handle dynamic, single-page application (SPA) sites that load content via JavaScript?","hard",("case-study",),
             ("It downloads the raw JS bundle and tries to read variable strings", "It skips dynamic sites entirely to save network bandwidth", "It asks the user to manually copy-paste the rendered text", "It runs a headless browser cluster (e.g. Puppeteer/Playwright) to execute the JS code, extracting DOM content post-render"), 3,
             "Headless browsing is expensive but necessary to crawl modern SPAs that do not serve pre-rendered HTML from the server."),
        quiz("wcrl-8","What is the role of the 'Coordinator Node' in a distributed web crawler?","medium",("case-study",),
             ("To assign URL partitions to specific crawler workers, track active nodes, and coordinate shared URL seen filters", "To balance HTTP requests across frontend web gateways", "To host search engines for users looking up index terms", "To encrypt raw HTML documents using distributed public key rings"), 0,
             "The coordinator distributes URLs (often sharded by domain hash) to worker nodes, preventing overlapping crawl attempts."),
        quiz("wcrl-9","What index optimizes looking up the crawl status of a target page URL?","easy",("case-study",),
             ("A partial index built only on pages that contain media assets", "A single index or hash index on the MD5/SHA256 hash of the URL string", "A composite index on (domain, crawled_at DESC)", "A hash index covering the page description"), 1,
             "Hashing the URL yields a fixed-size string. A hash index on the URL hash resolves seen lookups in O(1) time."),
        quiz("wcrl-10","Why is an asynchronous parser pipeline beneficial in a web crawler architecture?","medium",("case-study",),
             ("To delete historical user video uploads after 30 days", "To route WebSocket chat connections to the gateway servers", "To verify the user's credit card details during active scraping", "It allows crawler workers to fetch pages fast, pushing HTML to a queue for background parsers to extract links and text"), 3,
             "Decoupling downloading (network-bound) from parsing (CPU-bound) via messaging queues maximizes throughput and system utilization."),
        quiz("wcrl-11","What is the primary challenge of parsing huge binary files (like PDFs or ZIPs) during a web crawl?","medium",("case-study",),
             ("Relational databases cannot index binary columns", "They exhaust memory buffers and waste crawling cycles; resolved by checking Content-Type headers and bypassing non-HTML early", "They require installing custom hardware cards on every worker node", "They can only render in black-and-white formats on mobile devices"), 1,
             "Reading MIME headers allows crawlers to reject massive zip archives early, saving gigabytes of useless network transfers."),
        quiz("wcrl-12","What type of sharding distributes crawling tasks evenly across distributed crawler nodes?","hard",("case-study",),
             ("Shard alphabetically by the page's HTML length", "Sharding by host/domain name hash, routing all URLs for a target website to the same crawler instance to maintain politeness local locks", "Shard dynamically based on the length of the URL in characters", "Store all crawl records in a single partition hosted on an active SSD array"), 1,
             "Domain sharding makes local politeness delays easy to manage, as one node handles the specific domain queue completely."),
        quiz("wcrl-13","What is the benefit of a Mercator Crawler design?","hard",("case-study",),
             ("It automatically translates crawled pages into multiple languages", "It operates without consuming server CPU cycles", "It implements a modular URL Frontier separating priority queues from politeness queues using a clean mapping index", "It eliminates the need for maintaining relational database backups"), 2,
             "The Mercator model is the gold standard for web crawlers, featuring highly decoupled modules for prioritizer, frontier, and politeness."),
        quiz("wcrl-14","What type of locking is most appropriate when updating a domain's next crawl timestamp?","medium",("case-study",),
             ("Enforcing a database-wide table lock on the crawled table", "Distributed Redlock across all active global application servers", "Pessimistic locking (SELECT FOR UPDATE) to block all reads during the change", "Optimistic locking (version numbers) or local memory locks since a single domain queue is managed by one worker node"), 3,
             "Since domain routing keeps the queue on one crawler worker node, local thread locks are highly efficient, avoiding DB contention."),
        quiz("wcrl-15","What index optimizes looking up all pages crawled within a specific date range?","easy",("case-study",),
             ("A hash index built on the page author's name", "A clustered index sorting all pages globally by crawl date", "A single index on the page content text string", "A composite index on (domain, crawled_at DESC)"), 3,
             "A composite index on (domain, crawled_at DESC) allows the crawl monitor to query historical trends for any domain instantly."),
        quiz("wcrl-16","What is the purpose of the link extractor module in a web crawler?","easy",("case-study",),
             ("To encrypt video streaming files during transport layer security (TLS) handshakes", "To search HTML tags for href links and push them to the URL Frontier for future crawl scheduling", "To balance HTTP connections across frontend gateways", "To print physical mailing labels for page shipments"), 1,
             "The link extractor parses the page DOM, harvests anchor tags, normalizes relative links, and passes them to the frontier."),
        quiz("wcrl-17","What is the primary drawback of using high-frequency headless browser scraping?","hard",("case-study",),
             ("Headless browsers (Puppeteer) are extremely resource-heavy (high CPU and memory), limiting scalability to a few concurrent runs per node", "They are incompatible with standard cloud virtual machine servers", "They can only render in black-and-white formats on mobile devices", "They completely disable adaptive web streaming features"), 0,
             "Launching a chromium instance consumes significant RAM and CPU. HTTP GETs are 100x faster and lighter; use headless only when forced."),
        quiz("wcrl-18","How does the system handle spider traps (endless loops of dynamically generated URLs)?","hard",("case-study",),
             ("Immediately cancel the crawl and issue a full refund to the customer", "By applying heuristic rules: limiting URL folder depth, setting maximum pages per domain, and detecting structural URL loops", "Block all other deliveries in the system until the loop resolves", "Delete the domain's history and ban their IP address"), 1,
             "Spider traps crash naive crawlers. Limits on depth, length, and repeated path segments catch traps before memory runs out."),
        quiz("wcrl-19","What is the benefit of a WebSockets connection over HTTP short polling for crawl status dashboards?","medium",("case-study",),
             ("WebSockets reduce server connection overhead by avoiding repeated HTTP handshakes, header transmission, and DB checks", "WebSockets do not require SSL/TLS encryption protocols", "WebSockets execute crawls faster because they bypass compile steps", "WebSockets store crawl results directly in the client's RAM memory"), 0,
             "Real-time dashboards benefit from WebSockets pushing active logs from worker nodes directly to the screen with zero refresh lag."),
        quiz("wcrl-20","What index optimizes looking up all crawling failures sorted by error code?","hard",("case-study",),
             ("A hash index built on the worker's IP address", "A single index on the error description text string", "A composite index on (status, error_code) where status='FAILED'", "A clustered index sorting all failures globally by date"), 2,
             "A composite index on (status, error_code) allows support teams to query and group failures by their HTTP error codes instantly."),
    ))

def ad_click_aggregator_lesson() -> Lesson:
    return Lesson(id="hld-ad-aggregator", number=225, title="Ad Click Aggregator",
        focus=("stream ingestion","sliding window","event deduplication","clickhouse","backpressure"),
        overview="Design a high-throughput, real-time ad click aggregation and analytical platform.",
        patterns=(), badge="HLD Case Study", tasks=(
        quiz("adag-1","What is the primary challenge of designing an Ad Click Aggregator compared to standard analytics?","medium",("case-study",),
             ("The need to route clicks via physical Bluetooth networks", "The extreme scale of events (billions of clicks/day) and the absolute need for zero click loss due to financial billing impact", "The necessity of hosting HTML seat layout grids in memory", "The requirement to run LLM translations on click descriptions"), 1,
             "Click aggregation impacts billing. We must handle massive throughput (millions/sec) while guaranteeing exactly-once processing."),
        quiz("adag-2","Which storage system is highly optimized for storing and querying aggregated ad metrics?","medium",("case-study",),
             ("A local SQLite database loaded on client device smartphone screens", "An append-only flat log file saved to a shared network drive", "An OLAP database like ClickHouse or Apache Druid, optimized for fast columnar aggregations over billions of rows", "A standard MySQL database using multiple table-level locks"), 2,
             "Columnar OLAP databases excel at analytical queries, scanning billions of records in milliseconds for aggregations."),
        quiz("adag-3","How does the system ensure 'exactly-once' processing of ad click events?","hard",("case-study",),
             ("By locking the entire database during every single click write", "By forcing the client browser to verify the transaction in a separate HTTP thread", "By resetting the streaming worker servers every 5 seconds", "By assigning a unique idempotency key (click_id) to each event, tracking processed keys in a fast distributed dedup store"), 3,
             "Combining Kafka offsets with an in-memory deduplication cache (e.g. Redis) prevents double-counting clicks during retries."),
        quiz("adag-4","What is the difference between Hot Path and Cold Path in an analytical architecture?","hard",("case-study",),
             ("Hot path aggregates raw events in memory for real-time dashboards; Cold path writes raw logs to HDFS/S3 for batch/billing audits", "Hot path is for SSD arrays; Cold path is for tape storage", "Hot path requires symmetric keys; Cold path uses public key pairs", "Hot path is eventually consistent; Cold path is strongly consistent"), 0,
             "Hot path prioritizes low-latency (streaming). Cold path guarantees 100% accuracy and completeness (batch/Hadoop)."),
        quiz("adag-5","What technology is best suited for real-time stream aggregation of ad clicks?","medium",("case-study",),
             ("A standard cron task querying a relational database table", "Apache Flink or Spark Streaming, which aggregate events over sliding time windows (e.g., clicks per minute)", "An in-memory Redis cache with high-frequency snapshots", "A local shell script running on client devices"), 1,
             "Streaming engines aggregate pings in memory before writing to disk, protecting persistent stores from high-frequency writes."),
        quiz("adag-6","What is backpressure in a streaming pipeline, and how is it handled?","hard",("case-study",),
             ("Slowing down ingestion (or alerting upstream brokers to buffer data) when downstream databases are overwhelmed by write load", "Using redundant server instances to absorb traffic spikes dynamically", "A database index sweep that runs every 60 seconds", "Symmetric encryption protocols that encrypt data payloads"), 0,
             "If ClickHouse lag grows, Flink tells Kafka to slow down delivery, protecting downstream storage from crashing."),
        quiz("adag-7","Why is sharding click event data by Ad ID highly beneficial for stream aggregators?","hard",("case-study",),
             ("It distributes the exact same event across all physical nodes in parallel", "It ensures all click events for a specific ad route to the exact same streaming node, allowing highly efficient local aggregation", "It automatically encrypts video titles using distributed symmetric keys", "It eliminates the need for maintaining Redis memory keys for rate limiting"), 1,
             "Routing events by Ad ID means each worker node counts its subset locally in memory, preventing network shuffling bottlenecks."),
        quiz("adag-8","What is the thundering herd issue in ad click writes, and how is it avoided?","hard",("case-study",),
             ("When client devices reboot repeatedly to bypass rate limit checks", "When multiple gateways attempt to write to the same Redis master node concurrently, causing CPU spikes", "When Redis replication lag causes different gateways to see contradictory rate limit status logs", "When a streaming window resets, millions of aggregate writes hit the database simultaneously; mitigated by buffered batch flushes"), 3,
             "Batching writes in stream windows (e.g., flushing to DB every 10 seconds or 10,000 aggregated rows) smoothens the database write rate."),
        quiz("adag-9","How is the MapReduce pattern used in cold-path click reconciliation?","medium",("case-study",),
             ("MapReduce automatically compiles video streaming code files", "MapReduce runs daily batch jobs to compile exact, consolidated historical logs, removing duplicates and correcting streaming errors", "MapReduce handles real-time WebSocket connection state routing", "MapReduce encrypts the raw video files before storage"), 1,
             "Batch analytical engines process raw logs offline, generating highly accurate consolidated billing tables and auditing logs."),
        quiz("adag-10","Why are in-memory Redis Sorted Sets (ZSET) excellent for serving the top active ad campaigns in real-time?","medium",("case-study",),
             ("They keep elements pre-sorted by click count, enabling ultra-fast, low-latency O(1) reads of the top active ad list", "They can be hosted inside the client's local smartphone OS environment", "They automatically compress the video descriptions to save RAM", "They eliminate the need for maintaining relational database backups"), 0,
             "Redis ZSETs allow the system to quickly fetch and display the highest performing ad campaigns globally without hitting OLAP databases."),
        quiz("adag-11","What is the primary drawback of using a simple, single Redis counter (INCR) for every single ad click?","hard",("case-study",),
             ("INCR commands require complex relational database table locks", "A viral ad (e.g. 10,000 clicks/sec) will saturate the single Redis key, creating severe lock contention and network hotspots", "Redis automatically deletes counter keys after 24 hours of inactivity", "Redis counters are restricted to values under 1,000"), 1,
             "Hotspotting: high-frequency writes to a single Redis key overwhelm the single Redis thread. Solve via write-batching on web servers first."),
        quiz("adag-12","What type of sliding window is best for a 'Trending Ads' feed (updating every 10 seconds with the last 1 hour of data)?","hard",("case-study",),
             ("A session window that closes only when user activity stops", "A tumbling window that resets completely every 1 hour", "A sliding window with a length of 1 hour and a slide interval of 10 seconds", "A global window that accumulates data indefinitely without eviction"), 2,
             "Sliding windows overlap: they compute the last hour's trends and refresh the results every 10 seconds to keep the feed highly fresh."),
        quiz("adag-13","What is the benefit of a HyperLogLog data structure in ad click tracking?","hard",("case-study",),
             ("It encrypts user viewer histories for security compliance", "It automatically routes client requests to the nearest server", "It estimates the number of unique user clicks (cardinality) for an ad with near-perfect accuracy using negligible, fixed memory (12KB)", "It replaces the database for storing raw video files"), 2,
             "HyperLogLog estimates unique counts (e.g. daily active users clicking an ad) using fixed memory, avoiding storing massive sets of user IDs."),
        quiz("adag-14","How do you handle 'late-arriving data' (events delayed due to client offline states) in streaming engines?","hard",("case-study",),
             ("Discarding all delayed events automatically to prevent out-of-order errors", "Forcing all client devices to maintain a constant synchronized internet connection", "Rebooting the streaming cluster whenever delayed events are detected", "Using Watermarks, which tell the window processing engine when to close and evaluate a time window despite missing events"), 3,
             "Watermarks track event-time progress. A watermark of T allows the system to process events up to time T, ignoring or logging late-arrivers."),
        quiz("adag-15","What index optimizes looking up historical aggregated logs by date?","easy",("case-study",),
             ("A composite index on (aggregate_date, click_count DESC)", "A single index on the video title text string", "A partial index built only on videos containing ads", "A hash index built on the channel name"), 0,
             "A composite index on (aggregate_date, click_count DESC) allows the analytics engine to retrieve pre-sorted reports instantly."),
        quiz("adag-16","What is the role of the Event Broker (e.g. Kafka) in ad click ingestion?","medium",("case-study",),
             ("To write event logs to the client's phone filesystem", "To balance HTTP connections across frontend reverse proxies", "To ingest millions of raw click events, decouple them from backend processing, and serve as the immutable log for streaming aggregators", "To encrypt video streaming files during transport layer security (TLS) handshakes"), 2,
             "Kafka acts as the high-throughput buffer, allowing Flink, Spark, and MapReduce jobs to read click events concurrently without losing data."),
        quiz("adag-17","How can the system identify click-fraud (artificial bot clicks)?","medium",("case-study",),
             ("Requiring every viewer to solve a CAPTCHA puzzle before clicking an ad", "Deleting the ad if it accumulates more than 1M clicks in an hour", "Asynchronously analyzing click patterns, frequency, user agent, IP networks, and session history to filter out fraudulent events", "Enforcing a strict limit of one click per user per ad per lifetime"), 2,
             "An analytical spam pipeline runs out-of-band to flag and deduct bot clicks, ensuring advertisers are not billed for fraudulent traffic."),
        quiz("adag-18","What is the primary trade-off of a probabilistic data structure (like Count-Min Sketch) in analytics?","medium",("case-study",),
             ("It prevents athletes from editing their activity titles after posting", "It requires using symmetric encryption keys for every single query summary", "It saves massive RAM and CPU but introduces a small probability of overestimation (due to hash collisions)", "It works only on mobile device frameworks, not desktop web browsers"), 2,
             "Probabilistic structures sacrifice exact precision for extreme scale. For real-time estimation, small errors are highly acceptable."),
        quiz("adag-19","What index optimizes looking up all click events for an ad campaign inside a specific hour?","hard",("case-study",),
             ("A composite index on (campaign_id, click_time)", "A partial index built only on videos containing ads", "A single index on the user's password hash column", "A hash index built on the channel name"), 0,
             "A composite index on (campaign_id, click_time) allows the analytics engine to fetch and count the campaign's clicks within a specific hour instantly."),
        quiz("adag-20","What type of locking is most appropriate when updating an ad campaign's status from 'Active' to 'Paused'?","medium",("case-study",),
             ("Pessimistic locking (SELECT FOR UPDATE) to block all reads during the update phase", "Enforcing a database-wide table lock on the campaigns table", "Optimistic locking (version numbers) since status updates occur infrequently and have low contention", "Distributed Redlock across all active global application servers"), 2,
             "Since status updates are rare, optimistic locking avoids performance overhead and deadlocks, keeping the table fast for reads."),
    ))

def payment_system_lesson() -> Lesson:
    return Lesson(id="hld-payment", number=226, title="Payment System",
        focus=("ledger accounting","saga pattern","idempotency","gateway integration","reconciliation"),
        overview="Design a highly secure, consistent, and audit-ready global payment processing platform.",
        patterns=(), badge="HLD Case Study", tasks=(
        quiz("pay-1","Which database design pattern is absolute standard for tracking money movement to ensure auditability?","hard",("case-study",),
             ("Storing balances in fast in-memory Redis caches with no write history logs", "Double-entry bookkeeping, where every transaction is recorded as a debit in one account and an equal credit in another", "Flat log files generated on a shared network drive", "Single-entry dynamic rows that are updated in-place to save disk space"), 1,
             "Double-entry bookkeeping ensures money is never created or destroyed; every change has an equal and opposite counterparty write, guaranteeing a full audit trail."),
        quiz("pay-2","What is the role of an Idempotency Key in a payment API?","hard",("case-study",),
             ("To prevent charging a customer twice if they double-click a pay button or retry a failed network request", "To encrypt credit card numbers before they are saved to disk", "To bypass three-domain secure (3DS) authentication workflows", "To compress checkout payloads before transport layer transfers"), 0,
             "An idempotency key (typically a UUID) ensures the payment gateway processes a specific transaction exactly once, returning a cached response on retries."),
        quiz("pay-3","How is a distributed multi-service checkout transaction (e.g. Booking -> Deduct Cash -> Confirm Ticket) best coordinated?","hard",("case-study",),
             ("Writing all transactional states to a global CDN edge cache", "Letting the services update eventually without error handling or rollback logic", "Running a single massive Two-Phase Commit (2PC) that locks all databases globally until payment finishes", "Using the Saga Pattern, executing local transactions sequentially and triggering compensating rollbacks if any step fails"), 3,
             "Sagas scale well: they avoid long-lived database locks by orchestrating local transactions sequentially, running rollbacks on failures."),
        quiz("pay-4","What is the role of a Reconciliation Engine in a payment architecture?","medium",("case-study",),
             ("To compare internal ledger transactions against external payment gateway/bank statements to identify and flag discrepancies", "To compress historical transaction archives to save storage", "To verify the IP location of the customer", "To manage user session logs and billing dashboard pages"), 0,
             "Reconciliation runs out-of-band (e.g., daily) to guarantee that our internal state matches reality (the actual bank records)."),
        quiz("pay-5","How should the payment system handle transient gateway errors (e.g. HTTP 503 Service Unavailable)?","medium",("case-study",),
             ("Immediately fail the checkout, cancel the booking, and refund the customer", "Loop the request synchronously indefinitely until it succeeds, blocking the client connection", "Ignore the error and write 'Success' to the ledger table", "Retry the request using exponential backoff with jitter, ensuring the same idempotency key is passed"), 3,
             "Retrying with exponential backoff spreads the load, while jitter avoids thundering herd surges on the recovering gateway."),
        quiz("pay-6","What is the primary drawback of Two-Phase Commit (2PC) compared to Sagas for payment systems?","hard",("case-study",),
             ("2PC only works on relational databases, not NoSQL stores", "2PC is mathematically incapable of ensuring data consistency", "2PC requires using custom database hardware arrays", "2PC holds database locks until the coordinator commits, creating severe performance bottlenecks and deadlock risks"), 3,
             "2PC is a blocking protocol. If the coordinator goes offline mid-transaction, locks are held indefinitely, paralyzing the database."),
        quiz("pay-7","Why is a transactional Outbox Pattern recommended for publishing payment events?","hard",("case-study",),
             ("It routes all payments to the nearest geographical master shard", "It writes events to the database in the same transaction as the ledger update, ensuring reliable publishing to Kafka via CDC", "It automatically encrypts payment logs before Kafka routing", "It allows the system to bypass Redis session checks entirely"), 1,
             "The Outbox pattern prevents dual-write anomalies (e.g., database commits but message broker write fails), ensuring reliable delivery."),
        quiz("pay-8","How are sensitive credit card details stored to comply with PCI-DSS security standards?","hard",("case-study",),
             ("Store cards in a dedicated schema encrypted using a hardcoded password key", "Write card numbers to flat text files inside an encrypted folder on the server", "Save card information inside the client's local smartphone secure storage", "Offload card storage to a tokenization vault (or payment gateway), returning a secure token; never store raw cards on origin DBs"), 3,
             "Tokenization vaults isolate PCI compliance scope, keeping sensitive credit card details out of the application databases entirely."),
        quiz("pay-9","What type of index optimizes looking up all transactions for a user sorted by transaction date?","easy",("case-study",),
             ("A hash index on the payment method column", "A single index on the transaction status string", "A composite index on (user_id, created_at DESC)", "A partial index built only on failed checkouts"), 2,
             "A composite index on (user_id, created_at DESC) allows the ledger dashboard to render a user's transaction history instantly."),
        quiz("pay-10","What is the benefit of a payment routing engine?","medium",("case-study",),
             ("It automatically translates billing invoices into multiple languages", "It operates without consuming web server CPU cycles", "It eliminates the need for maintaining database backups", "It dynamically routes checkouts to different payment gateways (e.g. Stripe, Adyen) based on cost, geography, and availability"), 3,
             "Routing engines optimize transaction cost and success rates, automatically falling back to alternatives if a gateway is down."),
        quiz("pay-11","How should a system prevent double-submitting a payout batch to a bank?","hard",("case-study",),
             ("Delete the batch record from the database immediately after click", "Generate a unique batch checksum hash and use a distributed lock to prevent concurrent submissions of the same hash", "Ask the banking clerk to verify details over the phone before processing", "Force the application server to sleep for 5 minutes between payouts"), 1,
             "Checksum locks prevent concurrent or accidental re-submission of the same batch payload, shielding against catastrophic errors."),
        quiz("pay-12","Which mechanism handles webhook delivery retries from external gateways safely?","medium",("case-study",),
             ("Idempotency checks on the webhook receiver: verify the event_id has not been processed in the database before updating status", "Refuse all webhooks that arrive after business hours", "Reboot the receiver server after every webhook write to flush buffers", "Force client browsers to fetch webhook status via short-polling"), 0,
             "Webhooks guarantee at-least-once delivery, meaning they can repeat. Idempotency guards against repeating payment updates."),
        quiz("pay-13","What type of database isolation level is recommended when updating ledger account balances?","hard",("case-study",),
             ("Enforcing eventual consistency with eventual database triggers", "Read Committed isolation without locks to avoid thread blocking", "Serializable isolation or explicit row locks (SELECT FOR UPDATE) to guarantee absolute consistency and prevent write skew", "Read Uncommitted isolation level to maximize throughput speed"), 2,
             "Financial ledgers require strict mathematical consistency. Serializable or explicit row locks prevent overdraft anomalies."),
        quiz("pay-14","What index optimizes looking up all ledger entries for a specific account?","easy",("case-study",),
             ("A partial index built only on overdraft records", "A hash index built on the customer's email", "A single index on the balance amount", "A composite index on (account_id, entry_time DESC)"), 3,
             "A composite index on (account_id, entry_time DESC) optimizes fetching statements and balance logs chronologically."),
        quiz("pay-15","What is the purpose of compensating transactions in a Saga?","hard",("case-study",),
             ("To explicitly undo changes made by previous successful steps when a downstream step fails, restoring system balance", "To verify the physical location of the buyer's IP address", "To index transaction columns inside the relational database", "To encrypt checkout payloads during transport layer security (TLS) handshakes"), 0,
             "Sagas do not have a rollback feature like local DBs. Developers must write explicit code (compensations) to reverse edits."),
        quiz("pay-16","How does the ledger architecture handle historical balance audits (calculating net worth at a specific date)?","hard",("case-study",),
             ("Search client browser cookie history files", "Take the value of the active balance row directly from the user table", "Compute the balance by running a chronological SUM over all ledger entries up to that date, optimizing via stored snapshots", "Estimate the balance using linear regression models"), 2,
             "A ledger is an append-only log of changes. To get balance at time T, sum all entries. Snapshots at month-ends speed this up."),
        quiz("pay-17","What is the primary drawback of using pessimistic locking on a highly popular merchant balance row?","hard",("case-study",),
             ("It requires using specialized custom database hardware nodes", "It creates severe lock contention, slowing down all checkout requests for that merchant as they wait for the lock to release", "It is mathematically impossible to implement on relational databases", "It forces all transaction history records to be deleted on update failure"), 1,
             "When millions buy from one merchant (e.g. Amazon), locking their balance row stalls threads. Solve via buffer queues or child ledger sheets."),
        quiz("pay-18","What is a Split Ledger (or Multi-Ledger) model, and how does it optimize scaling?","hard",("case-study",),
             ("Duplicating every transaction across two separate database engines in parallel", "Dividing a hot account's balance updates across multiple sub-accounts to scale writes, merging them during reads", "Splitting ledger databases across different countries to bypass tax rules", "Storing ledgers on separate client browser devices"), 1,
             "Splitting hot accounts into N parallel shards distributes write locking, preventing the merchant balance row from becoming a bottleneck."),
        quiz("pay-19","What index optimizes looking up all chargebacks sorted by amount?","easy",("case-study",),
             ("A clustered index sorting all transactions by date", "A hash index built on the customer's credit card token", "A single index on the chargeback reason text string", "A composite index on (status, amount DESC) where status='CHARGEBACK'"), 3,
             "A composite index on (status, amount DESC) allows accounting teams to quickly locate and audit largest chargeback items."),
        quiz("pay-20","What is the role of a Settlement Service in a payment platform?","medium",("case-study",),
             ("To manage customer passwords and session keys", "To compile and batch internal transaction details, initiating fund transfers to merchant bank accounts periodically", "To host static HTML dashboard graphics", "To verify web socket connection handshakes"), 1,
             "Settlement batches transactions, coordinates with clearing networks (ACH/SEPA), and payouts the merchants off the real-time path."),
    ))

def metrics_monitoring_lesson() -> Lesson:
    return Lesson(id="hld-metrics", number=227, title="Metrics & Monitoring",
        focus=("pull vs push","timeseries DB","lsm-tree","cardinality explosion","alerting engine"),
        overview="Design a highly available, real-time metrics collection, monitoring, and alerting system.",
        patterns=(), badge="HLD Case Study", tasks=(
        quiz("mtr-1","What is a major difference between Pull and Push metric collection models (e.g. Prometheus vs InfluxDB)?","medium",("case-study",),
             ("Pull: monitoring server periodically scrapes metrics from targets; Push: targets actively transmit metrics to the server", "Pull is for SSDs; Push is designed exclusively for RAM caches", "Pull is eventually consistent; Push is strongly consistent", "Pull requires using symmetric keys; Push uses public key pairs"), 0,
             "Prometheus (pull) scrapes HTTP endpoints from a target list. InfluxDB/Graphite (push) receive metrics sent from active clients."),
        quiz("mtr-2","What is 'high cardinality' in monitoring, and why does it break standard time-series databases?","hard",("case-study",),
             ("When metric names contain special non-alphanumeric characters", "When metrics have unique dynamic labels (like user_id, ip), multiplying time-series paths exponentially, bloat indexing RAM", "When the CPU core temperatures exceed safety limits", "When monitoring logs are stored in flat text files on disk"), 1,
             "Dynamic labels create unique time-series keys. Millions of unique users mean millions of keys, overflowing memory and indexing layers."),
        quiz("mtr-3","Which storage engine design is highly optimized for high-throughput write streams in Time-Series Databases (TSDB)?","hard",("case-study",),
             ("Append-only text files shared on local network mount nodes", "Standard B-Trees that run recursive database locks on every write", "Hash indexes designed for exact coordinate pairings", "Log-Structured Merge-tree (LSM-Tree) variations, which write metrics sequentially to memory buffers, flushing to sorted files"), 3,
             "LSM-trees convert random writes to sequential disk writes, easily handling millions of data points per second."),
        quiz("mtr-4","What is the purpose of 'Rollups' (downsampling) in a metrics monitoring platform?","medium",("case-study",),
             ("To delete all metric tables after 24 hours of inactivity", "To balance HTTP connections across gateway routers", "To aggregate high-resolution historical data (e.g., 10s raw metrics) into lower resolutions (e.g., 5m, 1h) to save storage", "To encrypt metric strings before saving them to disk"), 2,
             "Downsampling compresses older metrics: keeping micro-resolution metrics forever is costly and unnecessary for long-term trends."),
        quiz("mtr-5","How does a Pull-based monitoring model locate dynamic microservice instances to scrape?","hard",("case-study",),
             ("By scanning the entire internet IP range on every crawl loop", "By letting targets register their connection keys in browser cookies", "By asking the developer to manually type in target IPs on every deploy", "By integrating with a Service Discovery registry (like Consul, ZooKeeper, or Kubernetes API) to track active endpoints"), 3,
             "Service discovery tracks dynamic instances. The pull monitor queries the registry, getting an active list of target nodes to scrape."),
        quiz("mtr-6","What is the role of an Alerting Engine in a metrics system?","medium",("case-study",),
             ("To evaluate metric streams against defined rule conditions (e.g., CPU > 90% for 5m) and dispatch alerts (PagerDuty, Slack)", "To manage user session logs and passwords", "To host analytical query interfaces for developers", "To transcode metric graphs into image formats"), 0,
             "The alerting engine continuously queries TSDB or checks live metric streams, flagging anomalies and managing alert fatigue."),
        quiz("mtr-7","How can you handle a massive spike in metrics traffic during a global application outage?","hard",("case-study",),
             ("Decouple collection via an ingestion queue (like Kafka) and apply adaptive downsampling or rate limiting at collector gates", "Store incoming metrics in local client browser databases", "Force all application virtual machines to reboot simultaneously", "Immediately shut down the monitoring server to prevent it from crashing"), 0,
             "An ingestion buffer queue (Kafka) shields the TSDB from write spikes, while rate limiters drop excessive high-frequency logs if needed."),
        quiz("mtr-8","What is the benefit of a columnar storage layout in time-series metric analysis?","hard",("case-study",),
             ("It optimizes analytical queries (e.g. average CPU over a week) by scanning only the target metric column, saving disk I/O", "It replaces the need for keeping cache databases in memory", "It automatically encrypts metric columns using distributed public key rings", "It is required to run background full-text searches"), 0,
             "Columnar storage co-locates values of a column on disk. Queries scanning a specific metric bypass unrelated data entirely."),
        quiz("mtr-9","Why is standard relational database pagination (OFFSET) bad for querying millions of time-series data points?","hard",("case-study",),
             ("OFFSET locks the entire database table during query execution", "OFFSET forces the client browser to reload all CSS styling files", "It is computationally too slow as OFFSET reads and discards all previous rows, degrading performance severely on deep pages", "Relational databases do not support page-based offset SQL statements"), 2,
             "For time-series data ranges, querying using timestamp boundaries (e.g. `timestamp > last_seen_time`) is infinitely more efficient."),
        quiz("mtr-10","How does Prometheus handle metrics storage locally?","hard",("case-study",),
             ("It stores metrics in a custom, highly optimized append-only local TSDB on disk, partitioning data into 2-hour blocks", "It logs metrics in flat CSV files inside user profile directories", "It keeps all historical data permanently in server RAM with no disk backup", "It writes metrics directly to a PostgreSQL database write replica"), 0,
             "Prometheus uses a highly efficient local TSDB, avoiding the network overhead of external databases for high-speed writes."),
        quiz("mtr-11","What is the purpose of 'Alert Deduplication' in a monitoring system?","medium",("case-study",),
             ("To group multiple related alert events (e.g., 100 containers firing 'DB Down') into a single consolidated notification", "To prevent developers from creating identical query rules", "To encrypt alert emails using distributed key rings", "To delete the alert history tables if a duplicate click is detected"), 0,
             "Deduplication (alert grouping) avoids pager storms, letting developers see the aggregate issue without getting buried in pings."),
        quiz("mtr-12","What type of index optimizes looking up metrics by label sets (e.g., finding metrics for env='prod', service='api')?","hard",("case-study",),
             ("A standard B-tree index on the metric name column", "A partial index built only on metrics containing values over 100", "An Inverted Index mapping label-value pairs to series IDs (similar to search engine indexes)", "A hash index on the value column"), 2,
             "TSDBs use inverted indexes to map labels (tags) to series IDs, enabling instant filtering of matching time-series records."),
        quiz("mtr-13","What is the primary trade-off of a short metric collection interval (e.g. 1 second)?","hard",("case-study",),
             ("It prevents servers from executing multi-step calculations", "It works only on mobile device frameworks, not desktop web browsers", "It provides extreme high-resolution observability but consumes massive disk storage, network bandwidth, and TSDB write CPU", "It requires using symmetric encryption keys for every single update summary"), 2,
             "Fine-grained monitoring creates massive data volumes. 10-60 second intervals are standard to balance storage costs."),
        quiz("mtr-14","What index optimizes looking up metric data points for a specific series ID within a date range?","easy",("case-study",),
             ("A hash index built on the collector IP address", "A partial index built only on metric values over 100", "A composite index on (series_id, timestamp DESC)", "A single index on the metric name string"), 2,
             "A composite index on (series_id, timestamp DESC) allows the query engine to instantly retrieve and graph metrics in range."),
        quiz("mtr-15","What is the benefit of Prometheus' federation feature?","hard",("case-study",),
             ("It automatically translates metric strings into multiple languages", "It allows parent Prometheus servers to scrape aggregated metrics from child Prometheus instances, scaling monitoring hierarchically", "It operates without consuming server CPU cycles", "It eliminates the need for maintaining relational database backups"), 1,
             "Federation enables horizontal scaling: local Prometheus servers monitor local zones, while a global master pulls aggregates."),
        quiz("mtr-16","What is the role of an exporter in Prometheus monitoring?","medium",("case-study",),
             ("To write event logs to the client's phone filesystem", "To collect metrics from third-party systems (like Redis, MySQL) that don't natively expose Prometheus metrics, converting them to HTTP format", "To encrypt metrics before they are written to S3 disks", "To balance HTTP connections across frontend reverse proxies"), 1,
             "Exporters act as adapters, wrapping legacy systems in HTTP metric interfaces that Prometheus can easily scrape."),
        quiz("mtr-17","How does the system ensure alerting engine resilience if the primary monitoring server goes offline?","hard",("case-study",),
             ("Immediately cancel all alerts and issue a full refund to the customer", "Delete the alert history and ban the device from the platform", "Block all other actions in the system until the server reconnects", "Run redundant alerting servers in active-passive or active-active consensus setups, using alertmanagers to de-duplicate fired pings"), 3,
             "High availability: multiple engines run rules concurrently, but a shared Alertmanager cluster handles deduplication and routing safely."),
        quiz("mtr-18","What is the primary advantage of Push-based metric collection for ephemeral workloads (e.g. serverless functions)?","medium",("case-study",),
             ("Pushing automatically encrypts target password credentials", "Ephemeral tasks spin down instantly; pushing metrics directly to a gateway is reliable, as pull scrapers can miss short-lived runs", "Pushing requires zero network connections to execute", "Pushing runs 100x faster than pull checks"), 1,
             "Serverless functions exist only for milliseconds. Pull crawlers cannot scrape them fast enough. Push-to-gateway is the standard solve."),
        quiz("mtr-19","What is the purpose of the 'Alertmanager' in Prometheus architecture?","medium",("case-study",),
             ("To balance HTTP gateway requests across distributed web servers", "To encrypt transaction payloads during billing calculations", "To host static HTML dashboard graphics", "To receive fired alerts from Prometheus, manage deduplication, grouping, silencing, inhibitions, and routing to downstream integrations"), 3,
             "Alertmanager specializes in notification management, ensuring developers get actionable, grouped alerts instead of duplicate spams."),
        quiz("mtr-20","What index optimizes looking up all active alerts in an Alertmanager database?","easy",("case-study",),
             ("A single index on the alert name string", "A hash index built on the target's IP address", "A composite index on (status, fired_at DESC) where status='ACTIVE'", "A partial index built only on alerts that failed to deliver"), 2,
             "A composite index on (status, fired_at DESC) allows the UI to render the active alerts list instantly pre-sorted."),
    ))

def chatgpt_lesson() -> Lesson:
    return Lesson(id="hld-chatgpt", number=228, title="ChatGPT (LLM serving at Scale)",
        focus=("LLM inference caching","KV cache","conversational routing","GPU load balancing","SSE streaming","model orchestration"),
        overview="Design a highly available, ultra-low latency Large Language Model serving infrastructure like ChatGPT.",
        patterns=(), badge="HLD Case Study", tasks=(
        quiz("gpt-1","What is the primary infrastructure bottleneck of serving large language models (LLMs) to millions of concurrent users?","medium",("case-study",),
             ("Relational database write speeds for storing conversation histories", "Static HTML asset hosting capacity of global CDN edge servers", "Web socket connection limits of API gateway networks", "GPU memory bandwidth and computational capacity during autoregressive generation (token-by-token generation)"), 3,
             "LLM serving is highly GPU-bound. Token generation requires loading model parameters to GPU memory for every token, creating severe memory bottlenecks."),
        quiz("gpt-2","What is 'KV Caching' in LLM inference, and why is it critical for scaling?","hard",("case-study",),
             ("Saving decrypted user chat logs in client browser local filesystems", "Caching stock price tickers in Redis memory keys for sparkline charts", "Caching static model weights at CDN edge locations", "Storing key-value attention pairs of previous tokens in GPU VRAM, avoiding recalculating past contexts for every new token"), 3,
             "Autoregressive models process all prior tokens to predict the next. Caching KV matrices avoids expensive O(N^2) attention recomputations."),
        quiz("gpt-3","What is the role of Semantic Caching in an LLM serving pipeline?","hard",("case-study",),
             ("Storing chat history in localized geographic partitions", "Hashing queries using MD5 to match exact string formats in Redis", "Translating queries into Base58 encoded string arrays", "Using vector databases to match incoming queries to cached responses with high similarity, bypassing GPU inference entirely"), 3,
             "Semantic caching uses embeddings to check if a highly similar query (e.g. 'explain photosyntheis' vs 'how does photosynthesis work') exists, saving massive GPU costs."),
        quiz("gpt-4","How does ChatGPT deliver response text dynamically in real-time rather than waiting for the entire block?","medium",("case-study",),
             ("Writing response fragments directly to a relational database write replica", "Forcing the client browser to refresh the entire page every 100 milliseconds", "Sending email fragments summarizing conversation statuses", "Streaming tokens to the client browser using Server-Sent Events (SSE) or WebSockets as they are generated by the GPU"), 3,
             "Streaming keeps perceived latency low: the client sees text flowing instantly via SSE without waiting 10+ seconds for full blocks."),
        quiz("gpt-5","How should the system route user prompts to handle multi-turn, stateful conversations?","hard",("case-study",),
             ("Forcing the client watch to send GPS coordinate streams", "Deleting historical conversation history after 5 minutes", "Routing all chats to the exact same GPU node using sticky sessions", "Session-based routing or keeping conversation history in a fast Redis cache, appending past context to the new prompt on every call"), 3,
             "LLMs are stateless. To support conversation state, the server must fetch historical chats from a fast cache and package them in the LLM prompt context."),
        quiz("gpt-6","What is the role of a GPU Load Balancer in an LLM cluster?","medium",("case-study",),
             ("To coordinate distributed locks during concurrent database updates", "To manage user session authentication tokens", "To index search engine keywords for fast profile matching", "To route inference requests based on active GPU memory utilization, token queue length, and model weights co-location"), 3,
             "Standard HTTP balancers are inadequate. GPU balancers optimize scheduling based on model sizes, KV cache states, and execution queues."),
        quiz("gpt-7","What is 'Continuous Batching' (or Orca batching) in LLM inference engine design?","hard",("case-study",),
             ("Writing transaction records in compressed binary batches to local SSDs", "Processing all requests sequentially to avoid concurrent lockups", "Dynamic iteration-level scheduling that batches multiple active requests together, releasing requests as soon as they finish", "Loading multiple model parameters to CPU RAM in parallel"), 2,
             "Since users request varying lengths, standard batching stalls. Continuous batching inserts new prompts dynamically at each token step."),
        quiz("gpt-8","Why is a NoSQL Wide-Column store (like Cassandra) ideal for storing long-term chat history records?","medium",("case-study",),
             ("It eliminates the need for maintaining Redis memory cache instances", "It automatically translates text inputs into multiple languages", "It supports complex recursive table JOINs required to calculate elevation trends", "It scales writes horizontally with ease, indexing rows by conversation_id and sorting entries chronologically by timestamp"), 3,
             "Chat history is classic sequential timeseries metadata. Cassandra's clustering keys optimize chronological retrieval of chat records."),
        quiz("gpt-9","What is a major security challenge when serving proprietary large language models?","medium",("case-study",),
             ("Downloading the entire historical training dataset to the client device", "Implementing prompt filtering and safety guardrails (both input and output moderation) in real-time before streaming answers", "Re-indexing the global relational database tables representing user phone numbers", "Encrypting the user's contact list using symmetric keys"), 1,
             "Safety pipelines run in milliseconds, filtering malicious prompts (jailbreaks) and preventing offensive model generations from rendering."),
        quiz("gpt-10","How can the serving system optimize GPU VRAM utilization when managing multiple models (e.g. GPT-4, GPT-3.5)?","hard",("case-study",),
             ("Forcing all models to share a single GPU core without partitioning", "Running all inference calculations in CPU RAM to free up VRAM", "Dynamic model routing: loading model weights in dedicated GPU nodes, sharding large models across multiple GPUs via tensor parallelism", "Converting all model parameters into Base58 string representations"), 2,
             "Large models exceed single GPU capacities. Tensor parallelism splits layers across adjacent GPUs linked by high-speed NVLink interconnects."),
        quiz("gpt-11","What is the primary benefit of a 'System Prompt' in chat applications?","easy",("case-study",),
             ("To provide an encryption key for client-side API payload decryptions", "To inject background instructions (e.g. safety rules, persona) into the model's context before appending user input", "To redirect the user's browser to an alternative server network", "To log client-side network error messages to the gateway host"), 1,
             "System prompts act as the underlying operating instructions, defining how the model behaves and responds to the subsequent user prompts."),
        quiz("gpt-12","What type of index optimizes looking up all conversations for a user sorted by active date?","easy",("case-study",),
             ("A partial index built only on conversations containing links", "A single index on the conversation title string", "A composite index on (user_id, updated_at DESC)", "A hash index built on the model name"), 2,
             "A composite index on (user_id, updated_at DESC) allows the chat history sidebar to load and sort conversations instantly."),
        quiz("gpt-13","What is the purpose of the Tokenizer in an LLM serving pipeline?","easy",("case-study",),
             ("To balance network connections across gateway nodes", "To run full-text search index updates on the database", "To convert raw text characters into numerical token IDs that the neural network can process, and vice-versa", "To encrypt user password strings before storage"), 2,
             "Tokenizers convert text to vector tokens and convert generated numerical outputs back to readable words for streaming."),
        quiz("gpt-14","How do you scale real-time chat search over millions of conversation logs?","medium",("case-study",),
             ("Run raw SQL 'LIKE %query%' queries on every user search request", "Create a single massive B-tree index covering every column in the table", "Write search queries to local client cookies and execute scans in JavaScript", "Index conversation logs in a search engine like Elasticsearch, syncing records asynchronously from Cassandra"), 3,
             "Elasticsearch provides lightning-fast full-text search, allowing users to search their historical chat logs with high performance."),
        quiz("gpt-15","What is the advantage of using a Page-based KV cache allocation (like vLLM)?","hard",("case-study",),
             ("It manages GPU VRAM dynamically by partitioning KV cache memory into pages, eliminating internal VRAM fragmentation", "It automatically compiles Python code directly into native machine assembly", "It returns matching chat IDs faster by caching them in Redis memory", "It automatically scales database write nodes based on execution queues"), 0,
             "Standard allocations allocate maximum sequence lengths, wasting 60-80% VRAM. Virtual page allocations (vLLM) optimize VRAM by 99%."),
        quiz("gpt-16","How should the system handle model inference failures during an active user chat stream?","medium",("case-study",),
             ("Detect connection drop or GPU error, failover to a fallback replica node, and resume streaming using the cached KV state", "Reboot the entire active GPU cluster automatically", "Ignore the crash and show the user an eternal cursor blink", "Delete the user's history and ban their IP address"), 0,
             "Robust serving frameworks detect failures instantly and re-route requests without frustrating the user with full restarts."),
        quiz("gpt-17","What is the benefit of a Semantic Cache over standard String Cache?","hard",("case-study",),
             ("It does not require keeping vector embeddings in memory", "It requires advanced relational database triggers to resolve", "It is computationally faster to calculate on modern hardware", "It matches semantically identical queries with varying wording (e.g. 'what is rain' vs 'explain rain'), maximizing cache hits"), 3,
             "Standard cache misses if a single character changes. Semantic caches check semantic closeness, serving similar queries instantly."),
        quiz("gpt-18","What is the role of a Vector Database (like Pinecone or Milvus) in custom LLM systems?","medium",("case-study",),
             ("To write event logs to the client's phone filesystem", "To store and query high-dimensional vector embeddings of documents to retrieve relevant contexts for RAG pipelines", "To balance HTTP connections across frontend reverse proxies", "To encrypt video streaming files during transport layer security (TLS) handshakes"), 1,
             "Retrieval-Augmented Generation (RAG) uses vector databases to locate relevant files matching user prompts, injecting them in context."),
        quiz("gpt-19","What type of locking is most appropriate when updating a conversation's title?","medium",("case-study",),
             ("Enforcing a database-wide table lock on the conversations table", "Pessimistic locking (SELECT FOR UPDATE) to block all reads during the update phase", "Distributed Redlock across all active global application servers", "Optimistic locking (version numbers) since conversation title changes are rare and have zero user contention"), 3,
             "Since conversation title updates are rare, optimistic locking guarantees integrity without performance overhead."),
        quiz("gpt-20","What index optimizes looking up all message logs for a conversation sorted by order?","easy",("case-study",),
             ("A single index on the message content text string", "A clustered index sorting all messages globally by date", "A hash index built on the user's email address", "A composite index on (conversation_id, message_order ASC)"), 3,
             "A composite index on (conversation_id, message_order ASC) allows the chat UI to retrieve and render the entire thread in causal order."),
    ))

# Mappings of the new lessons to their respective URLs
NOTES_BY_LESSON_MAPPING3 = {
    "hld-google-docs": "https://www.hellointerview.com/learn/system-design/problem-breakdowns/google-docs",
    "hld-dist-cache": "https://www.hellointerview.com/learn/system-design/problem-breakdowns/distributed-cache",
    "hld-youtube": "https://www.hellointerview.com/learn/system-design/problem-breakdowns/youtube",
    "hld-job-scheduler": "https://www.hellointerview.com/learn/system-design/problem-breakdowns/job-scheduler",
    "hld-web-crawler": "https://www.hellointerview.com/learn/system-design/problem-breakdowns/web-crawler",
    "hld-ad-aggregator": "https://www.hellointerview.com/learn/system-design/problem-breakdowns/ad-click-aggregator",
    "hld-payment": "https://www.hellointerview.com/learn/system-design/problem-breakdowns/payment-system",
    "hld-metrics": "https://www.hellointerview.com/learn/system-design/problem-breakdowns/metrics-monitoring",
    "hld-chatgpt": "https://www.hellointerview.com/learn/system-design/problem-breakdowns/chatgpt",
}
