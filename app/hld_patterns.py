"""HLD Patterns: Real-time Updates, Dealing with Contention, Multi-step Processes, Scaling Reads — 20 MCQs each."""
from __future__ import annotations
from .lld_practice import quiz, Lesson

def realtime_updates_lesson() -> Lesson:
    return Lesson(id="hld-realtime-updates", number=109, title="Real-time Updates",
        focus=("WebSockets","SSE","long polling","pub/sub","push vs pull","event-driven"),
        overview="Real-time updates are asked about in nearly every interview. Know the protocols and when to use each.",
        patterns=(), badge="HLD Pattern", tasks=(
        quiz("rt-1","What are the three main approaches for real-time updates?","medium",("real-time",),("Polling, long polling, and persistent connections (WebSockets/SSE)","A testing approach that injects controlled failures to verify system recovery behavior","An eviction algorithm that considers both recency and frequency when selecting victims","A load shedding strategy that drops low-priority requests when resource utilization is high"),0,"Polling (wasteful), long polling (server holds request), WebSockets/SSE (persistent connection)."),
        quiz("rt-2","When should you use WebSockets over SSE?","hard",("real-time",),("When you need bidirectional communication (chat, gaming) — SSE is server-to-client only","A prefetching algorithm that predicts future access patterns from recent query history","A resilience pattern that isolates failures in one subsystem from propagating to others","A mechanism that compresses HTTP headers to reduce bandwidth on repeated requests"),0,"WebSockets: bidirectional (chat). SSE: unidirectional server→client (live scores, notifications)."),
        quiz("rt-3","What is the fan-out problem in real-time systems?","hard",("real-time",),("When one event must be delivered to millions of subscribers simultaneously","An index maintenance operation that rebuilds fragmented pages for better scan performance","A failure detection mechanism using heartbeat intervals and configurable timeout thresholds","A sharding approach that distributes cached entries across nodes by consistent hashing"),0,"Solutions: tiered fan-out, push for most users + pull for celebrity followers, connection pooling."),
        quiz("rt-4","How does Redis pub/sub work for real-time?","medium",("real-time",),("Publishers send to channels; subscribers receive messages instantly — fire-and-forget","A security protocol that authenticates both client and server during the handshake","A replication technique that mirrors hot cache entries across multiple nodes for availability","A pattern where incoming requests are buffered and processed in priority-ordered batches"),0,"Redis pub/sub is lightweight but doesn't persist messages. Missed messages are gone."),
        quiz("rt-5","What is the difference between pub/sub and message queues for real-time?","hard",("real-time",),("Pub/sub broadcasts to all subscribers; queues deliver to one consumer per message","Queues are real-time only across multiple service instances","A data structure that stores cache metadata compactly using probabilistic counting","A transaction isolation level that prevents phantom reads using predicate locking"),0,"Pub/sub: one-to-many broadcast. Queue: one-to-one competing consumers."),
        quiz("rt-6","How do you handle millions of concurrent WebSocket connections?","hard",("real-time",),("Distribute across many servers with sticky sessions or a presence service tracking which server handles which user","A serialization format that reduces cache entry size through schema-aware compression","A monitoring approach that aggregates metrics from multiple sources into unified dashboards","A transport layer optimization that reduces round-trip overhead using multiplexing"),0,"Use a presence service (Redis) to track user→server mapping. Route messages accordingly."),
        quiz("rt-7","What is a presence service?","medium",("real-time",),("A system tracking which users are online and which server they're connected to","A state synchronization approach that reconciles differences using Merkle tree comparison","A data pipeline stage that transforms and enriches events before writing to storage","A backup mechanism that captures incremental changes since the last full snapshot"),0,"Presence services (Redis/ZooKeeper) maintain user→server mappings for routing real-time messages."),
        quiz("rt-8","How does Slack/Discord handle real-time message delivery?","hard",("real-time",),("WebSocket connections per client, gateway servers handling connections, internal message routing via pub/sub","A connection management approach that pins clients to specific backend instances","A storage engine optimization that pre-sorts data on disk for sequential access patterns","A process that periodically reconciles state between systems to detect and fix drift"),0,"Gateway servers maintain WS connections. Internal pub/sub routes messages between gateways."),
        quiz("rt-9","What is connection multiplexing in real-time systems?","hard",("real-time",),("Multiple logical channels share a single physical connection (e.g., Socket.IO rooms)","A load balancing technique with automatic failover support","A flow control mechanism that prevents the sender from overwhelming the receiver","One connection per request with automatic failover support"),0,"Multiplexing reduces connection overhead. One WS connection can subscribe to multiple topics/rooms."),
        quiz("rt-10","What is the heartbeat mechanism in WebSockets?","medium",("real-time",),("Periodic ping/pong messages to detect dead connections and keep firewalls/proxies from closing idle connections","A protocol that batches multiple requests into a single TCP segment for efficiency","A DNS-based technique that distributes queries across multiple authoritative servers","A technique where the server pre-fetches resources based on predicted client needs"),0,"Without heartbeats, dead connections go undetected, wasting server resources."),
        quiz("rt-11","How do you handle message ordering in distributed real-time systems?","hard",("real-time",),("Use sequence numbers per channel/user, buffer out-of-order messages, and deliver in sequence","A clock synchronization technique that uses NTP with bounded drift for ordering events","A technique that partitions workload across worker pools based on resource requirements","Messages are always ordered using configurable thresholds"),0,"Distributed systems can deliver messages out of order. Client-side reordering with sequence numbers fixes this."),
        quiz("rt-12","What is the difference between push and pull models for news feeds?","hard",("real-time",),("Push: precompute feeds on post (fan-out on write). Pull: compute at read time (fan-out on read)","A replication technique that synchronously mirrors writes to a standby for zero data loss","A communication pattern where services exchange messages through a centralized broker","A connection management approach that limits concurrent sessions to prevent overload"),0,"Push for regular users. Pull for celebrities. Hybrid is best."),
        quiz("rt-13","What is event-driven architecture?","medium",("real-time",),("Components communicate by producing and consuming events asynchronously via a message broker","A replication strategy where writes go to any node and conflicts are resolved on read","A schema design pattern that embeds frequently accessed data to avoid join overhead","Synchronous request/response using configurable thresholds"),0,"Event-driven decouples producers from consumers, enabling scalability and loose coupling."),
        quiz("rt-14","What is MQTT and when is it used?","hard",("real-time",),("A lightweight publish-subscribe protocol designed for IoT and mobile devices with unreliable networks","A congestion avoidance algorithm that dynamically adjusts the transmission window","A monitoring tool that tracks hit rates, eviction counts, and memory usage per cache pool","A membership protocol where nodes periodically exchange state with random cluster peers"),0,"MQTT is lightweight (small header), supports QoS levels, and handles unreliable connections well."),
        quiz("rt-15","How does Firebase Realtime Database handle real-time sync?","hard",("real-time",),("WebSocket connections with path-based subscriptions; server pushes only changed data (diffs)","A consensus protocol that uses a rotating leader to distribute write coordination load","A deployment strategy that gradually shifts traffic from the old version to the new one","A scheduling algorithm that assigns tasks to workers based on estimated completion time"),0,"Firebase sends only data diffs, reducing bandwidth. Clients subscribe to specific paths in the JSON tree."),
        quiz("rt-16","What is the challenge of real-time updates at scale?","hard",("real-time",),("Maintaining millions of persistent connections, efficient message routing, and handling connection churn","A partitioning algorithm that assigns data based on a hash of the geographic region","A partitioning strategy that splits data based on a hash of the composite primary key","A routing strategy that directs traffic based on geographic proximity of the client"),0,"At scale: connection management, efficient pub/sub routing, and graceful handling of reconnections."),
        quiz("rt-17","What is the difference between at-most-once and at-least-once delivery in real-time?","hard",("real-time",),("At-most-once may lose messages; at-least-once may deliver duplicates but never loses","At-most-once is more reliable across multiple service instances","At-least-once loses messages using configurable thresholds","A warming strategy that pre-loads data based on access frequency from the previous day"),0,"Chat apps need at-least-once + deduplication. Notifications can tolerate at-most-once."),
        quiz("rt-18","How do you implement typing indicators in a chat app?","medium",("real-time",),("Send ephemeral WebSocket events that are NOT persisted — they're purely transient signals","A consistency model where reads from any replica are guaranteed to be monotonically fresh","A monitoring tool that tracks query execution plans and identifies slow running queries","A coordination service that uses ephemeral nodes for automatic failure detection"),0,"Typing indicators are fire-and-forget signals. No persistence needed — if missed, no harm done."),
        quiz("rt-19","What is the role of a message broker in real-time systems?","medium",("real-time",),("Decouples producers from consumers, buffers messages, and enables async processing","A cache coherence protocol that broadcasts invalidation messages to all cache tiers","A query optimization strategy that rewrites subqueries into more efficient join patterns","A persistence mechanism that snapshots the cache state to disk for crash recovery"),0,"Brokers (Kafka, RabbitMQ, Redis Streams) provide reliability, ordering, and fan-out capabilities."),
        quiz("rt-20","How does gRPC server streaming work for real-time?","hard",("real-time",),("Server sends a stream of messages over a single HTTP/2 connection; client receives them sequentially","By sending polling requests repeatedly to a REST endpoint in the background","By establishing a raw UDP socket connection between the client and the server","By storing message payloads in a Redis queue and pulling them on client request"),0,"gRPC streaming: server opens a response stream. Efficient binary serialization via Protobuf."),
    ))

def contention_lesson() -> Lesson:
    return Lesson(id="hld-contention", number=110, title="Dealing with Contention",
        focus=("optimistic locking","pessimistic locking","CAS","distributed locks","hot partitions"),
        overview="Contention is the enemy of scale. Know when to lock, when to avoid locks, and how to design around hotspots.",
        patterns=(), badge="HLD Pattern", tasks=(
        quiz("cnt-1","What is optimistic concurrency control?","medium",("contention",),("Assume no conflict; check version at commit time and retry if conflict detected","Always locking the database table during a transaction to prevent concurrent modifications","Creating read-only views of the table to allow high-concurrency read operations","Splitting a database table into multiple vertical partitions to avoid write conflicts"),0,"Optimistic works well with low contention. Higher throughput than pessimistic locking."),
        quiz("cnt-2","What is pessimistic locking?","medium",("contention",),("Lock the resource upfront before any operation to prevent conflicts","Allowing concurrent writes to proceed and resolving conflicts later using vector clocks","Caching all write requests in a message broker queue to process them sequentially","Automatically retrying transactions when a write collision is detected at commit time"),0,"SELECT FOR UPDATE in SQL. Prevents conflicts but reduces throughput due to lock contention."),
        quiz("cnt-3","What is compare-and-swap (CAS)?","hard",("contention",),("An atomic operation: read current value, compare with expected, swap only if match","A sorting algorithm that optimizes memory layouts for array operations","A data compression algorithm that reduces duplicate keys in memory tables","A load balancing algorithm that routes traffic to the server with the least connections"),0,"CAS is the foundation of lock-free data structures. Redis WATCH/MULTI implements CAS semantics."),
        quiz("cnt-4","How do you handle ticket purchasing contention (Ticketmaster)?","hard",("contention",),("Temporary seat reservation with TTL + atomic inventory decrement (Redis DECR or DB row lock)","Allowing all transactions to proceed and resolving seat double-bookings after payment","Routing all users to a single database master node without any queueing or locking","Generating random seating assignments during payment processing to avoid collisions"),0,"Reserve with TTL (10 min), decrement atomically, release on timeout or payment completion."),
        quiz("cnt-5","What is the hot partition problem?","hard",("contention",),("One partition/shard receiving disproportionate traffic, becoming a bottleneck","A network routing bottleneck where physical routers overheat due to excessive load","An indexing error where data becomes corrupted due to improper hashing functions","A memory allocation error where the cache runs out of space for temporary tables"),0,"Solutions: add entropy to key (salting), split the hot key, or use write-behind buffering."),
        quiz("cnt-6","What is distributed locking?","hard",("contention",),("Coordinating mutual exclusion across multiple nodes using a distributed lock service","Locking a local file descriptor on a single web server to prevent multi-thread access","Routing traffic exclusively to a single application instance to avoid concurrent processing","Configuring a database clustering protocol to prevent replication lag"),0,"Implemented via Redis (Redlock), ZooKeeper, or etcd. More complex and less reliable than local locks."),
        quiz("cnt-7","What is the Redlock algorithm?","hard",("contention",),("Martin Kleppmann's critique: acquire locks on N/2+1 independent Redis instances with a global timeout","A native Redis CLI command used to execute transactions atomically","A distributed consensus protocol like Raft designed to choose cluster leaders","A cryptographic hashing function used to secure user passwords in Redis"),0,"Redlock attempts distributed locking but is controversial — clock drift and GC pauses can break safety."),
        quiz("cnt-8","What is idempotency and how does it help with contention?","hard",("contention",),("Making operations safe to retry — if contention causes failure, the client retries without side effects","A sorting algorithm that maintains the relative order of equal elements in a collection","A caching technique that prevents the database from performing repeated read queries","A locking strategy that prevents multiple clients from acquiring the same resource"),0,"Idempotency keys let servers detect duplicates, making retry-on-conflict safe."),
        quiz("cnt-9","How do you handle the counter problem at scale?","hard",("contention",),("Shard the counter across N shards, increment random shard, sum all shards for total (approximate)","Locking the single counter database row during every increment request","Routing all increment requests to a single dedicated memory cache instance","Executing transactions under the serializable isolation level for all increments"),0,"Single counters don't scale. Sharded counters trade accuracy for throughput."),
        quiz("cnt-10","What is the leaky bucket algorithm for handling contention?","hard",("contention",),("Requests flow in at any rate but are processed at a fixed rate — excess requests are dropped or queued","A caching policy that discards oldest keys when memory capacity is exceeded","A distributed locking mechanism that releases resources after a fixed timeout","A memory allocation scheme that reclaims unused object segments dynamically"),0,"Leaky bucket smooths bursty traffic. Unlike token bucket, it enforces a strict constant output rate."),
        quiz("cnt-11","How does Stripe handle payment contention?","hard",("contention",),("Idempotency keys + optimistic locking + event sourcing ensure exactly-once payment processing","Rejecting all retry attempts and requiring the customer to submit a new card","Processing all payments sequentially through a single thread to avoid concurrency","Locking the entire customer account table during the payment transaction"),0,"Each payment has a unique idempotency key. Retries check the key and return the existing result."),
        quiz("cnt-12","What is write skew and how do you prevent it?","hard",("contention",),("Two transactions read overlapping data, make decisions, and write — each valid individually but together violate a constraint","A user interface rendering delay caused by high database write latency","A read performance bottleneck where database pages are read out of order","A caching error where the read cache diverges from the underlying database state"),0,"Example: two doctors both see 3 on-call, each decides to remove themselves, leaving 1. Fix: serializable isolation."),
        quiz("cnt-13","What is a semaphore vs a mutex?","medium",("contention",),("Mutex allows one holder; semaphore allows N concurrent holders (counting semaphore)","A semaphore is used in local processes, while a mutex is used across network clusters","A mutex is a hardware lock, while a semaphore is a software-level lock implementation","A mutex allows multiple readers, whereas a semaphore allows only a single writer"),0,"Use mutex for exclusive access. Use semaphore to limit concurrency (e.g., max 10 DB connections)."),
        quiz("cnt-14","How do you handle auction bid contention?","hard",("contention",),("Atomic compare-and-swap: read current highest → compare → update only if new bid is higher","Locking the entire auction database table during every bid submission","Allowing all bids to write to the database and resolving conflicts manually later","Routing all bid traffic to a single thread that updates bids sequentially in memory"),0,"CAS pattern: ensure only the highest valid bid wins without race conditions."),
        quiz("cnt-15","What is the thundering herd problem in locking?","hard",("contention",),("When a lock is released, all waiting threads wake up and compete for it, causing CPU spikes","An audio processing issue when streaming media under high packet loss conditions","A network routing failure where all DNS queries resolve to the same server","A database replication bottleneck where replicas pull transaction logs simultaneously"),0,"Solution: use fair locks (FIFO queue) or exponential backoff with jitter."),
        quiz("cnt-16","What is a read-write lock?","medium",("contention",),("Allows multiple concurrent readers OR a single writer — improves throughput for read-heavy workloads","A database transaction isolation level that prevents dirty reads and writes","A physical file permission system that separates read and write users","A load balancing strategy that routes reads and writes to different target servers"),0,"RW locks: many readers share access. Writers get exclusive access. Readers block only during writes."),
        quiz("cnt-17","How does MVCC handle contention?","hard",("contention",),("Each transaction sees a snapshot of the data, allowing concurrent reads/writes without blocking","By replicating data across multiple database instances to distribute write load","By routing all writes to a single thread that updates table rows sequentially","By locking the table row during reads and releasing it during write operations"),0,"MVCC: readers don't block writers, writers don't block readers. Each sees a consistent snapshot."),
        quiz("cnt-18","What is exponential backoff with jitter?","medium",("contention",),("Retry with exponentially increasing delays plus random jitter to spread retries and reduce contention","A thread scheduling algorithm that yields CPU execution time to higher priority tasks","A persistent connection retry loop that keeps connections alive using heartbeats","A database query timeout setting that scales proportionally with the active transaction count"),0,"Wait = min(cap, base × 2^attempt) + random_jitter. Prevents synchronized retry storms."),
        quiz("cnt-19","What is the inventory reservation pattern?","hard",("contention",),("Reserve items with a TTL during checkout; release if payment fails or times out","Locking the entire inventory database table during the checkout checkout process","A batch script that counts warehouse physical inventory levels once per day","Allowing all purchases to proceed and canceling orders if stock runs out"),0,"Prevents overselling while allowing concurrent checkouts. TTL-based reservations auto-expire."),
        quiz("cnt-20","What is the saga pattern for handling distributed contention?","hard",("contention",),("A sequence of local transactions with compensating actions — avoids distributed locks entirely","A transaction isolation level that serializes all writes across shards","A distributed consensus protocol that elects a leader to coordinate locks","A locking scheme where a coordinator node locks all services before proceeding"),0,"Sagas replace distributed locks with local transactions + compensations. No 2PC blocking needed."),
    ))

def multistep_lesson() -> Lesson:
    return Lesson(id="hld-multistep", number=111, title="Multi-step Processes",
        focus=("sagas","2PC","event sourcing","outbox pattern","compensating actions","workflow engines"),
        overview="Distributed transactions are inevitable. Know Sagas, 2PC, event sourcing, and when to use each.",
        patterns=(), badge="HLD Pattern", tasks=(
        quiz("ms-1","What is the Saga pattern?","hard",("saga",),("A sequence of local transactions where each step has a compensating action for rollback","A transaction isolation level that prevents dirty reads in distributed systems","A two-phase commit protocol that blocks participant services until all acknowledge","A caching pattern where reads are routed to the nearest regional replication node"),0,"Sagas handle distributed transactions without 2PC. Each step can be undone independently."),
        quiz("ms-2","Choreography vs orchestration sagas?","hard",("saga",),("Choreography: services react to events. Orchestration: a central coordinator directs steps","Choreography is centralized via a message broker; orchestration is completely decentralized","Choreography is designed for read-heavy workloads; orchestration is optimized for writes","Orchestration uses physical timers, whereas choreography relies on logical clocks"),0,"Choreography is more decoupled. Orchestration is centralized and easier to reason about."),
        quiz("ms-3","What is two-phase commit (2PC)?","hard",("distributed",),("Prepare phase (all vote) → commit phase (all commit or all abort). Atomic but blocking","A client-side load balancing strategy that distributes queries across two replicas","A caching policy that requires validation from two servers on a hit","A database indexing scheme that uses dual-sorted B-trees for fast query paths"),0,"2PC guarantees atomicity but blocks if coordinator fails. Avoided in microservices."),
        quiz("ms-4","Why is 2PC problematic in microservices?","hard",("distributed",),("Blocking nature, coordinator SPOF, high latency, and tight coupling between services","It lacks transaction atomicity, leading to frequent data corruption across nodes","It is incompatible with asynchronous message brokers like RabbitMQ or Kafka","It requires all database instances to be of the exact same vendor and version"),0,"2PC blocks all participants if coordinator fails. Sagas are preferred for loose coupling."),
        quiz("ms-5","What is event sourcing?","hard",("event-sourcing",),("Storing all state changes as an immutable event log instead of just current state","A logging framework that writes application stack traces to a centralized server","A message broker configuration that ensures events are delivered exactly once","A database sharding technique that distributes event tables across regional nodes"),0,"Event sourcing gives audit trails, time travel, and ability to rebuild state from events."),
        quiz("ms-6","What is the outbox pattern?","hard",("patterns",),("Write events to an outbox table in the same DB transaction, then publish asynchronously","A caching strategy where expired values are kept in memory for quick retrieval","An email delivery service configuration that queues messages prior to SMTP sending","A logging technique where debug messages are written to stdout instead of files"),0,"Outbox ensures atomicity between DB writes and event publishing. Avoids dual-write problems."),
        quiz("ms-7","What is a compensating transaction?","medium",("saga",),("An action that undoes the effect of a previous transaction step (e.g., cancel booking, refund payment)","A manual administrative operation executed to repair a corrupt database table","A database rollback operation that undoes a transaction using the redo log","An eviction command that clears a cache bucket when memory limits are hit"),0,"In Sagas, compensations undo completed steps when a later step fails."),
        quiz("ms-8","What is the CDC (Change Data Capture) approach to multi-step?","hard",("patterns",),("Streaming DB changes as events via the WAL, enabling downstream systems to react asynchronously","A backup process that copies database snapshots to remote storage regularly","A schema migration tool that updates table structures across database clusters","A caching strategy that intercepts query calls to populate cache layers"),0,"CDC (Debezium) turns DB changes into events without modifying application code."),
        quiz("ms-9","How do you handle partial failures in a Saga?","hard",("saga",),("Execute compensating transactions for completed steps in reverse order","Locking all downstream services to prevent concurrent modifications during the error","Ignoring the failed steps and marking the overall transaction as successful anyway","Re-running the entire saga from the beginning, including completed steps"),0,"If step 3 of 5 fails: compensate step 2, compensate step 1. Each compensation must be idempotent."),
        quiz("ms-10","What is the transactional outbox implementation?","hard",("patterns",),("Write to outbox table in same DB transaction as business data; a poller/CDC reads outbox and publishes events","A cache-aside pattern where the application updates the cache and DB concurrently","A local file system directory where log files are stored prior to compression","A message broker queue configuration that automatically processes transactions"),0,"Guarantees at-least-once event publishing without distributed transactions."),
        quiz("ms-11","What are the challenges of event sourcing?","hard",("event-sourcing",),("Schema evolution, event versioning, replay performance, and eventual consistency of read models","The inability to perform read operations without replaying all events from history","Tight coupling between the database schema and the application code","Higher storage cost since events must be duplicated across multiple databases"),0,"Event stores grow forever. Snapshots help. Event schema changes require migration strategies."),
        quiz("ms-12","What is a process manager in orchestration sagas?","hard",("saga",),("A stateful coordinator that tracks saga progress, sends commands, and handles responses/timeouts","A Unix operating system process daemon that monitors container health states","A load balancer routing rule that pins client sessions to specific instances","A database partition engine that maintains the order of transaction logs"),0,"The process manager maintains saga state and drives the workflow forward."),
        quiz("ms-13","What is idempotency's role in multi-step processes?","hard",("saga",),("Every step must be idempotent so that retries after partial failures don't cause side effects","A client-side UI optimization that disables submit buttons after one click","It has no role, as all multi-step workflows are coordinated by atomic locks","An optimization that routes duplicate requests to a cache instead of the database"),0,"Without idempotency, retrying a failed saga step could duplicate charges, bookings, etc."),
        quiz("ms-14","What is the dead letter queue in multi-step processes?","medium",("patterns",),("Where messages that fail processing after max retries are sent for investigation","A backup queue used to replicate messages across multiple availability zones","A logging system that records all network timeout exceptions to a file","A database storage segment that holds deleted table rows prior to vacuuming"),0,"DLQs prevent poison messages from blocking the pipeline."),
        quiz("ms-15","How do you ensure exactly-once processing in sagas?","hard",("saga",),("At-least-once delivery + idempotent consumers = effectively-once processing","By implementing a single master node that processes all transactions sequentially","By using a two-phase commit protocol across all participant database shards","By relying on the underlying TCP packet delivery guarantees to prevent duplicates"),0,"True exactly-once is nearly impossible. Idempotent handlers make duplicates harmless."),
        quiz("ms-16","What is the difference between choreography and orchestration in reliability?","hard",("saga",),("Choreography is harder to debug (implicit flow); orchestration provides clear visibility and error handling","Choreography is always more reliable since it has no message broker dependencies","Orchestration is less reliable due to its reliance on decentralized consensus algorithms","They are equivalent in reliability, differing only in query execution performance"),0,"Choreography: implicit flow in event subscriptions. Orchestration: explicit, traceable state machine."),
        quiz("ms-17","What is a workflow engine (e.g., Temporal, Cadence)?","hard",("patterns",),("A platform for defining, executing, and recovering durable multi-step workflows with built-in retry/state management","A load balancer that routes requests based on application CPU utilization","A message queue that implements priority delivery queues for urgent jobs","A build automation server used to compile and deploy application packages"),0,"Temporal/Cadence handle timeouts, retries, compensations, and state persistence automatically."),
        quiz("ms-18","What is the try-confirm-cancel (TCC) pattern?","hard",("patterns",),("A reservation pattern: Try (reserve), Confirm (commit), or Cancel (release) — each service manages its own resources","A caching policy that tries reading from local cache, then remote, then database","A two-phase commit protocol implementation supported natively by SQL databases","A network socket handshake protocol that confirms connection parameters"),0,"TCC is like Saga but with explicit reservation. Each service locks resources during Try phase."),
        quiz("ms-19","How does event sourcing relate to CQRS?","hard",("event-sourcing",),("Events drive the write model; read models are projections of events optimized for queries — naturally complementary","CQRS is a subset of event sourcing that replaces the write-ahead log with table views","They are identical patterns, just named differently by different database vendors","They cannot be used together because event sourcing requires a normalized write model"),0,"Event sourcing provides the event log. CQRS projects it into read-optimized materialized views."),
        quiz("ms-20","What is the at-least-once delivery guarantee and why is it the practical choice?","hard",("patterns",),("At-least-once + idempotent consumers is practical; exactly-once is nearly impossible","All delivery guarantees are mathematically equivalent under standard TCP/IP","Exactly-once is easier to implement but uses significantly more network bandwidth","At-most-once is preferred in financial systems since it prevents duplicate charges"),0,"At-least-once is achievable with acks/retries. Combined with idempotent processing, it's effectively-once."),
    ))

def scaling_reads_lesson() -> Lesson:
    return Lesson(id="hld-scaling-reads", number=112, title="Scaling Reads",
        focus=("read replicas","caching","CQRS","CDN","denormalization","materialized views"),
        overview="Most applications are read-heavy. Know the full toolkit for scaling reads efficiently.",
        patterns=(), badge="HLD Pattern", tasks=(
        quiz("sr-1","What is the most common approach to scaling reads?","easy",("scaling-reads",),("Add read replicas — write to primary, read from replicas","Creating more composite indexes on all query paths","Upgrading the primary database server CPU and RAM","Merging tables together to reduce the total database size"),0,"Read replicas distribute read load. Works well for read-heavy workloads with tolerable replication lag."),
        quiz("sr-2","What is CQRS?","hard",("scaling-reads",),("Command Query Responsibility Segregation — separate read and write models optimized independently","A database index type optimized for sorting range queries","A caching strategy that routes reads to local memory partitions","A load balancing algorithm that routes traffic based on server queue lengths"),0,"CQRS: write model can be normalized for consistency; read model can be denormalized for speed."),
        quiz("sr-3","How does denormalization help read performance?","medium",("scaling-reads",),("Pre-joins data into a single read-optimized structure, eliminating JOIN queries at read time","It has no impact on read performance but speeds up write operations","It reduces database storage size by eliminating duplicate columns","It forces the database engine to cache all index pages in memory"),0,"Denormalization trades write complexity and storage for dramatically faster reads."),
        quiz("sr-4","What is a materialized view?","medium",("scaling-reads",),("A precomputed query result stored as a table, updated periodically or on write","A cached query path stored in the memory layer of the application","A virtual index that maps table rows to physical disk addresses","A standard database view that runs its underlying query on every access"),0,"Materialized views: precomputed aggregations/JOINs. Great for dashboards and analytics."),
        quiz("sr-5","What is the replication lag problem?","hard",("scaling-reads",),("Time delay between a write on the primary and when it appears on replicas, causing stale reads","A physical routing failure where replicas lose network connection to the master","A database constraint error caused by concurrent writes on different shards","An eviction problem where cache nodes drop keys before replicas are updated"),0,"Solution: read-your-own-writes by routing the writing user to the primary for subsequent reads."),
        quiz("sr-6","When is caching better than read replicas?","medium",("scaling-reads",),("When you need sub-millisecond reads, data access patterns follow 80/20, and slight staleness is OK","Under no circumstances, as read replicas offer lower query latency","When data is written frequently and reads must be strongly consistent","In all scenarios since memory caches are easier to scale than databases"),0,"Cache: μs latency, handles hot data well. Replicas: ms latency, serve all data."),
        quiz("sr-7","What is the query result cache pattern?","medium",("scaling-reads",),("Cache the full result of expensive queries using the query+params as the cache key","A database index that caches table rows alphabetically","A virtual table view that stores query paths in memory","A write-through caching configuration that pre-populates all keys"),0,"Cache key = hash(query + params). Invalidate when underlying data changes."),
        quiz("sr-8","How does CDN help scale reads?","easy",("scaling-reads",),("Caches static content at edge locations near users, reducing origin server load","Routing API requests to the closest regional database primary instance","Replicating transactional tables to edge nodes for real-time joins","Scaling the write throughput of the primary database server"),0,"CDNs serve images, CSS, JS, and even API responses from edge — reducing latency and origin load."),
        quiz("sr-9","What is read-through caching in the context of scaling reads?","medium",("scaling-reads",),("The cache transparently fetches from DB on miss — application only talks to the cache","A write-behind caching configuration where updates are buffered in memory","A cache pattern identical to cache-aside except that the client writes to DB first","A database replication mode where replicas pull directly from the cache"),0,"Read-through simplifies application code — the cache handles the DB interaction."),
        quiz("sr-10","What is the fan-out on read approach?","hard",("scaling-reads",),("Computing the result at read time by querying and aggregating from multiple sources","Pre-computing customized user feeds in the database during post creation","A database scaling approach that partitions table writes across nodes","A caching strategy that pre-fetches data before the user requests it"),0,"Fan-out on read: cheaper writes but more expensive reads. Good for data that's written by celebrities."),
        quiz("sr-11","How do you handle search scaling for reads?","hard",("scaling-reads",),("Use a dedicated search engine (Elasticsearch) as a denormalized read model","Scaling the primary SQL database horizontally using read replicas","Increasing the CPU and RAM allocation of the search gateway proxy","Adding more B-tree indexes to search text fields in the relational database"),0,"Elasticsearch as a read-optimized projection: sync data via CDC, serve search/filter queries."),
        quiz("sr-12","What is pre-computation for scaling reads?","medium",("scaling-reads",),("Compute expensive results offline and store them for fast retrieval (e.g., recommendation scores, leaderboards)","Calculating query results in real-time on the client using WebAssembly","Optimizing network routes to reduce round-trip latency to the database","Pre-populating cache entries during the application startup routine"),0,"Pre-compute: leaderboards, recommendation scores, trending topics. Serve from cache/fast store."),
        quiz("sr-13","How do read replicas handle failover?","hard",("scaling-reads",),("Promote a replica to primary when the primary fails; other replicas switch to the new primary","Replicas do not participate in failover and must be manually recreated","By routing all client queries to the cache until the primary recovers","By automatically restarting the primary database server process"),0,"Failover promotes a replica. Some systems (RDS Multi-AZ) handle this automatically."),
        quiz("sr-14","What is the N+1 read problem and how does it affect scaling?","hard",("scaling-reads",),("Fetching a list (1 query) then N individual queries for related data — turns 1 query into N+1","A network routing bug that repeats API calls N times to the gateway","A database replication lag issue where writes are delayed by N seconds","A caching problem where misses cause N write updates to the database"),0,"N+1 multiplies database load. Fix with JOINs, batch fetching, or DataLoader pattern."),
        quiz("sr-15","What is edge computing for read scaling?","hard",("scaling-reads",),("Processing and serving data at edge locations closer to users, reducing round-trip latency","A caching protocol that distributes key-value pairs across CDN nodes","A database engine feature that moves indexes to SSD partitions","Running heavy analytics queries on user devices to save cloud costs"),0,"Edge computing (Cloudflare Workers, Lambda@Edge): run logic at CDN edge, not just cache content."),
        quiz("sr-16","How do you scale read-heavy time-series queries?","hard",("scaling-reads",),("Use time-based partitioning + downsampling + tiered storage (hot/warm/cold)","Adding composite B-tree indexes to the timestamp and metric columns","Upgrading the primary database server physical CPU and RAM","Storing all metric data in a single monolithic table to simplify joins"),0,"Recent data in fast store (SSD), older data downsampled and moved to cheaper storage."),
        quiz("sr-17","What is data locality optimization for reads?","medium",("scaling-reads",),("Storing related data that's frequently accessed together physically close (same partition, same node)","Storing all application data in a single centralized datacenter node","A caching strategy that stores duplicate database entries in local RAM","A network protocol optimization that compresses API payloads"),0,"Co-locating related data reduces cross-partition reads and improves cache utilization."),
        quiz("sr-18","When should you NOT add read replicas?","medium",("scaling-reads",),("When write-heavy workload means replicas can't keep up, or consistency requirements don't tolerate lag","Under no circumstances, as read replicas are always beneficial for scaling","When using non-relational databases since NoSQL does not support replication","When read queries are simple key-value lookups that can be cached easily"),0,"Write-heavy workloads: replicas lag behind, serving stale data. Consider caching or sharding instead."),
        quiz("sr-19","What is a query plan cache?","hard",("scaling-reads",),("Database caches parsed/optimized query plans to avoid re-planning identical queries","A database cache that holds index structures for fast primary path lookup","A disk storage segment where table rows are cached alphabetically","A memory cache that stores the actual string results of previous queries"),0,"Plan caching (prepared statements) eliminates query parsing overhead for repeated queries."),
        quiz("sr-20","How does pagination help scale reads?","easy",("scaling-reads",),("Breaks large result sets into pages, reducing memory usage and response size per request","It has no scaling benefit but is required for user interface design","It improves transaction consistency by locking fewer table partitions","It increases write speeds by reducing the number of rows per page"),0,"Without pagination, returning 1M rows crashes clients and overwhelms servers."),
    ))
