"""HLD Core Concepts: Sharding, Consistent Hashing, CAP Theorem, Numbers to Know — 20 MCQs each."""
from __future__ import annotations
from .lld_practice import quiz, Lesson

def sharding_lesson() -> Lesson:
    return Lesson(id="hld-sharding", number=105, title="Sharding",
        focus=("shard keys","range vs hash","hotspots","rebalancing","cross-shard queries"),
        overview="Sharding is how you scale databases horizontally. The shard key choice makes or breaks the design.",
        patterns=(), badge="HLD Core", tasks=(
        quiz("sh-1","What is database sharding?","easy",("sharding",),("Splitting a database across multiple machines, each holding a subset of data","A prefetching algorithm that predicts future access patterns from recent query history","A replication technique that mirrors hot cache entries across multiple nodes for availability","An eviction algorithm that considers both recency and frequency when selecting victims"),0,"Sharding distributes rows across nodes so no single machine handles all traffic."),
        quiz("sh-2","What makes a good shard key?","hard",("sharding",),("High cardinality, even distribution, frequently used in queries, rarely changing","A security protocol that authenticates both client and server during the handshake","A membership protocol where nodes periodically exchange state with random cluster peers","A data structure that stores cache metadata compactly using probabilistic counting"),0,"A good shard key distributes data evenly and supports common query patterns."),
        quiz("sh-3","What is a hot shard?","hard",("sharding",),("A deployment strategy that gradually shifts traffic from the old version to the new one","A sharding approach that distributes cached entries across nodes by consistent hashing","A shard receiving disproportionate traffic due to poor key distribution","A consistency model where reads from any replica are guaranteed to be monotonically fresh"),2,"Hot shards occur when a key value is too popular. Fix: composite keys, salting, range splitting."),
        quiz("sh-4","Range-based vs hash-based sharding?","medium",("sharding",),("A pattern where incoming requests are buffered and processed in priority-ordered batches","Range doesn't work with strings across multiple service instances","A clock synchronization technique that uses NTP with bounded drift for ordering events","Range assigns key ranges (good for scans); hash assigns by hash (good for distribution)"),3,"Range supports range queries but risks hotspots. Hash distributes evenly but makes range queries expensive."),
        quiz("sh-5","What is a cross-shard query?","hard",("sharding",),("A communication pattern where services exchange messages through a centralized broker","A query needing data from multiple shards, requiring scatter-gather and coordination","A monitoring tool that tracks hit rates, eviction counts, and memory usage per cache pool","A monitoring approach that aggregates metrics from multiple sources into unified dashboards"),1,"Cross-shard queries fan out to multiple nodes and require merging. Very expensive."),
        quiz("sh-6","What is shard rebalancing?","medium",("sharding",),("A scheduling algorithm that assigns tasks to workers based on estimated completion time","Moving data between shards when load becomes uneven or shards are added/removed","A technique where the server pre-fetches resources based on predicted client needs","A partitioning strategy that splits data based on a hash of the composite primary key"),1,"Rebalancing redistributes data — expensive, must be done carefully to avoid downtime."),
        quiz("sh-7","Why is auto-increment ID bad as a shard key?","hard",("sharding",),("A storage engine optimization that pre-sorts data on disk for sequential access patterns","Sequential IDs route all new writes to the same shard, creating a hotspot","A connection management approach that pins clients to specific backend instances","A testing approach that injects controlled failures to verify system recovery behavior"),1,"Sequential keys concentrate recent writes. Use UUIDs, Snowflake IDs, or hash-based assignment."),
        quiz("sh-8","What is directory-based sharding?","medium",("sharding",),("A warming strategy that pre-loads data based on access frequency from the previous day","A central service maps each key to its shard, allowing flexible placement at extra hop cost","A replication strategy where writes go to any node and conflicts are resolved on read","A cache coherence protocol that broadcasts invalidation messages to all cache tiers"),1,"Directory-based offers maximum flexibility but the directory becomes a SPOF."),
        quiz("sh-9","How does sharding affect JOINs?","hard",("sharding",),("A transport layer optimization that reduces round-trip overhead using multiplexing","JOINs across shards become very expensive or impossible — denormalize or co-locate","A process that periodically reconciles state between systems to detect and fix drift","A load shedding strategy that drops low-priority requests when resource utilization is high"),1,"Fundamental tradeoff: sharding sacrifices JOIN capability."),
        quiz("sh-10","What is geo-sharding?","medium",("sharding",),("An index maintenance operation that rebuilds fragmented pages for better scan performance","Partitioning data by geographic region so users' data is stored near them","A state synchronization approach that reconciles differences using Merkle tree comparison","A flow control mechanism that prevents the sender from overwhelming the receiver"),1,"Geo-sharding reduces latency and helps with data residency regulations (GDPR)."),
        quiz("sh-11","What is scatter-gather?","hard",("sharding",),("A partitioning algorithm that assigns data based on a hash of the geographic region","Send query to all shards (scatter), collect results, merge and return (gather)","A transaction isolation level that prevents phantom reads using predicate locking","A DNS-based technique that distributes queries across multiple authoritative servers"),1,"Scatter-gather is necessary when the shard key isn't in the query."),
        quiz("sh-12","Sharding vs partitioning — what's the difference?","medium",("sharding",),("Partitioning is general concept; sharding distributes partitions across different machines","A serialization format that reduces cache entry size through schema-aware compression","Partitioning is for reads only with automatic failover support","A monitoring tool that tracks query execution plans and identifies slow running queries"),0,"A single PostgreSQL can have partitions (one machine). Sharding distributes across nodes."),
        quiz("sh-13","How does Instagram generate sharded IDs?","hard",("sharding",),("Snowflake-like: timestamp + shard ID + sequence number — unique, sortable, shard-aware","A persistence mechanism that snapshots the cache state to disk for crash recovery","A failure detection mechanism using heartbeat intervals and configurable timeout thresholds","A replication technique that synchronously mirrors writes to a standby for zero data loss"),0,"41 bits timestamp + 13 bits shard ID + 10 bits sequence = globally unique, chronologically sortable."),
        quiz("sh-14","What is data co-location in sharding?","medium",("sharding",),("Placing related data on the same shard to enable efficient local JOINs and transactions","A consensus protocol that uses a rotating leader to distribute write coordination load","A resilience pattern that isolates failures in one subsystem from propagating to others","Storing all on one server using configurable thresholds"),0,"Co-location avoids cross-shard operations. Example: shard by user_id for all user data."),
        quiz("sh-15","What happens to transactions in a sharded DB?","hard",("sharding",),("Single-shard transactions work; cross-shard require 2PC or Saga patterns","A connection management approach that limits concurrent sessions to prevent overload","A backup mechanism that captures incremental changes since the last full snapshot","A coordination service that uses ephemeral nodes for automatic failure detection"),0,"Cross-shard transactions are expensive (2PC) or must be redesigned as eventual consistency workflows."),
        quiz("sh-16","What is resharding?","hard",("sharding",),("A congestion avoidance algorithm that dynamically adjusts the transmission window","A routing strategy that directs traffic based on geographic proximity of the client","Changing the sharding scheme, requiring massive data migration with minimal downtime","A query optimization strategy that rewrites subqueries into more efficient join patterns"),2,"Resharding is painful. Plan your shard key carefully upfront to avoid it."),
        quiz("sh-17","For a social network, why shard by user_id?","hard",("sharding",),("Most queries are user-centric (my feed, posts) so co-locating user data avoids cross-shard queries","A protocol that batches multiple requests into a single TCP segment for efficiency","A mechanism that compresses HTTP headers to reduce bandwidth on repeated requests","A schema design pattern that embeds frequently accessed data to avoid join overhead"),0,"User-centric access patterns dominate social networks."),
        quiz("sh-18","What is the challenge of unique constraints across shards?","hard",("sharding",),("A data pipeline stage that transforms and enriches events before writing to storage","Unique constraints are auto using configurable thresholds","Each shard only sees its data; global uniqueness requires centralized check or clever key design","A technique that partitions workload across worker pools based on resource requirements"),2,"Options: Snowflake IDs, centralized uniqueness service, or inherently unique key design."),
        quiz("sh-19","What is virtual sharding?","hard",("sharding",),("Each physical node owns multiple virtual positions on the hash ring for better load distribution","A staging database setup that mirrors production sharding for load testing","Dynamically creating temporary database schemas in memory for run-time query routing","Running multiple database engines in nested virtual machines on a single physical host"),0,"Virtual nodes improve balance in consistent hashing by giving each node multiple ring positions."),
        quiz("sh-20","What is a shard proxy?","medium",("sharding",),("A routing layer that directs queries to the correct shard based on the shard key","A database client library that intercepts raw queries and manually performs client-side joins","A hardware firewall that blocks queries containing unauthorized SQL statements","A replication coordinator that handles failover elections among read replicas"),0,"Shard proxies (e.g., Vitess for MySQL) handle routing, connection pooling, and query rewriting."),
    ))

def consistent_hashing_lesson() -> Lesson:
    return Lesson(id="hld-consistent-hashing", number=106, title="Consistent Hashing",
        focus=("hash ring","virtual nodes","rebalancing","load distribution","key mapping"),
        overview="Consistent hashing minimizes key redistribution when nodes are added/removed. Essential for distributed caches and databases.",
        patterns=(), badge="HLD Core", tasks=(
        quiz("ch-1","What problem does consistent hashing solve?","medium",("consistent-hashing",),("When nodes are added/removed, only a small fraction of keys need remapping (vs modulo remapping all)","Ensuring transaction consistency across multiple independent databases","Compressing hash values to optimize storage in high-volume indexes","Encrypting network payloads to prevent man-in-the-middle attacks"),0,"With modulo hashing, adding a node remaps almost all keys. Consistent hashing remaps only ~1/N."),
        quiz("ch-2","How does a consistent hash ring work?","medium",("consistent-hashing",),("Nodes and keys are hashed onto a circular ring; each key is assigned to the first node clockwise from it","A linked index list where nodes are sorted alphabetically by their network hostname","A binary search tree where each child node points to a range of client IP addresses","A hash map that uses linear probing to resolve collisions when assigning keys to slots"),0,"The ring is [0, 2^32). Hash both node IPs and keys. Walk clockwise from key's position to find its node."),
        quiz("ch-3","What are virtual nodes (vnodes)?","hard",("consistent-hashing",),("Each physical node maps to multiple positions on the hash ring for better load distribution","Lightweight containers running inside physical servers to isolate application processes","DNS aliases mapped to a pool of backend servers to enable round-robin load balancing","Logical partitions inside a database engine designed to store transaction logs"),0,"Without vnodes, nodes with different capacities or unlucky hash positions get uneven load."),
        quiz("ch-4","How many virtual nodes per physical node is typical?","hard",("consistent-hashing",),("100-200 vnodes per physical node for good distribution","Exactly 2 vnodes to cover the primary and backup positions on the hash ring","Exactly 1 vnode since mapping more positions degrades hash lookup time to O(N)","Between 10,000 and 50,000 vnodes to guarantee near-perfect load balance"),0,"More vnodes = smoother distribution. Cassandra defaults to 256 tokens per node."),
        quiz("ch-5","What happens when a node is added to the ring?","medium",("consistent-hashing",),("Only keys between the new node and its predecessor are moved to the new node","All keys on all nodes are re-hashed to determine their new target hosts","The client must clear its entire cache block to force reload from the database","The ring pauses all write operations until a full synchronization protocol completes"),0,"Only ~1/N keys move on average. This is the key advantage over modulo hashing."),
        quiz("ch-6","What happens when a node is removed?","medium",("consistent-hashing",),("Its keys are reassigned to the next node clockwise on the ring","The entire hash ring is invalidated and must be rebuilt by a master coordinator","Every key in the system is redistributed using modulo arithmetic","All client connections are permanently terminated until a new host is provisioned"),0,"Only the failed node's keys move. All other assignments remain stable."),
        quiz("ch-7","Why does modulo hashing (key % N) fail when N changes?","medium",("consistent-hashing",),("Changing N (nodes) remaps almost every key to a different node, causing massive cache hits/misses","It generates non-numeric hash keys that cause runtime type errors","It requires synchronous locking across all nodes to compute the remainder","It increases the computational complexity of lookups from O(1) to O(N)"),0,"Going from 3→4 nodes: 2/3 of keys remap. Consistent hashing: only 1/4."),
        quiz("ch-8","Which systems use consistent hashing?","medium",("consistent-hashing",),("DynamoDB, Cassandra, Memcached, CDNs, and distributed caches","Monolithic relational databases like Oracle and SQL Server","Standard message brokers like RabbitMQ when routing messages to single consumers","File systems like HDFS and GFS that use a single master coordinator node"),0,"It's fundamental to any distributed system that partitions data across nodes."),
        quiz("ch-9","How does consistent hashing relate to replication?","hard",("consistent-hashing",),("Keys can be replicated to the next N nodes clockwise on the ring for fault tolerance","Replication is handled by replicating keys to all virtual positions of the same physical node","Replication is only possible if all nodes are placed in the same availability zone","Replication requires synchronous two-phase commits to all nodes on the hash ring"),0,"DynamoDB replicates each key to 3 nodes clockwise on the ring (preferring distinct physical nodes)."),
        quiz("ch-10","What is the load balancing challenge with consistent hashing?","hard",("consistent-hashing",),("Without virtual nodes, nodes can get very uneven loads due to random hash distribution","The hash ring takes O(N) space per key, creating memory bottlenecks","Calculating the cryptographic hash of keys introduces significant CPU overhead","Clients cannot cache server locations, forcing a lookup for every request"),0,"With K physical nodes and no vnodes, standard deviation of load is O(1/√K) — too high for small K."),
        quiz("ch-11","How does DynamoDB use consistent hashing?","hard",("consistent-hashing",),("Partitions data across storage nodes with virtual nodes and preference lists for replication","By hashing partition keys to determine which physical table partition receives the item","By organizing tables into a global B-tree index where leaf nodes are sorted by partition key","By routing all writes to a single master coordinator that distributes keys round-robin"),0,"DynamoDB's consistent hashing with vnodes and coordinator-based routing. Described in the Dynamo paper."),
        quiz("ch-12","What is a preference list in DynamoDB's consistent hashing?","hard",("consistent-hashing",),("The ordered list of nodes responsible for storing replicas of a key, skipping same-rack nodes","A list of secondary indices that a query should prioritize for performance","A client-side configuration mapping specific tables to regional endpoints","A set of cached keys that the application accesses most frequently"),0,"The preference list ensures replicas land on distinct physical nodes for fault tolerance."),
        quiz("ch-13","How does consistent hashing handle heterogeneous nodes?","hard",("consistent-hashing",),("Assign more virtual nodes to higher-capacity machines so they handle proportionally more keys","By adjusting the hash function's output range dynamically based on node load","By routing smaller data objects to slower nodes and larger objects to faster nodes","By clustering slower nodes together into a separate sub-ring for failover"),0,"A 2x capacity node gets 2x vnodes, receiving roughly 2x the traffic."),
        quiz("ch-14","What is jump consistent hash?","hard",("consistent-hashing",),("A hash algorithm that maps keys to buckets uniformly with O(ln n) time and no ring overhead","A hash function that randomly jumps between multiple rings to locate keys","A routing protocol that forwards client requests through intermediate hop nodes","A sharding algorithm that shifts keys to the next available server using linear probing"),0,"Jump hash is simpler and more uniform than ring-based consistent hashing, but only supports sequential bucket IDs."),
        quiz("ch-15","What is rendezvous (highest random weight) hashing?","hard",("consistent-hashing",),("Each key computes a score for each node; the highest-scoring node wins — no ring needed","A protocol where nodes negotiate key ownership by sharing CPU load metrics","A hashing scheme that routes keys to the node with the lowest current connection count","A sharding mechanism that uses range partitioning combined with consistent hashing"),0,"Rendezvous hashing: simpler conceptually but O(N) per lookup. Good for small N."),
        quiz("ch-16","How does Cassandra use consistent hashing?","hard",("consistent-hashing",),("Each node owns token ranges on the ring; data is placed based on partition key hash","By partitioning tables into a global B-tree index where leaf nodes are sorted","By routing all writes to a master node that coordinates replicas","By hashing clustering keys to distribute rows within a single partition"),0,"Cassandra uses Murmur3 hash of the partition key to place data on the ring."),
        quiz("ch-17","What is the impact of consistent hashing on cache hit rates during scaling?","hard",("consistent-hashing",),("Adding a node causes only ~1/N cache misses instead of near-total cache invalidation","It degrades hit rates because the hash ring must be cleared and rebuilt","It completely invalidates the cache since all keys are remapped","It increases hit rates by caching all key-value pairs on multiple servers"),0,"This is why CDNs and distributed caches use consistent hashing — scaling doesn't destroy cache."),
        quiz("ch-18","How does consistent hashing handle rack/zone awareness?","hard",("consistent-hashing",),("Skip vnodes in the same rack when building replica sets to ensure fault tolerance across failure domains","By placing all replicas on the same rack to minimize network latency","By assigning different hash rings to different availability zones","By using a master coordinator node to physically copy files between zones"),0,"Network topology-aware placement ensures replicas survive rack/zone failures."),
        quiz("ch-19","What is bounded load consistent hashing?","hard",("consistent-hashing",),("Variant where each node has a max load cap — if a node is overloaded, keys spill to the next node","A firewall configuration that drops requests when CPU usage exceeds 90%","A load balancing strategy that assigns keys to the node with the lowest network latency","A caching mechanism that evicts keys when memory usage reaches a threshold"),0,"Google's bounded load variant prevents hotspots by redistributing overflow keys."),
        quiz("ch-20","What is the relationship between consistent hashing and DHTs?","hard",("consistent-hashing",),("DHTs (Chord, Kademlia) use consistent hashing as the foundation for decentralized key-value lookup","Consistent hashing is used to encrypt the messages routed through DHTs","DHTs use consistent hashing to dynamically elect a single master directory node","Consistent hashing is only used in centralized DHT architectures to balance query load"),0,"DHTs extend consistent hashing with routing tables for O(log N) lookups in peer-to-peer systems."),
    ))

def cap_theorem_lesson() -> Lesson:
    return Lesson(id="hld-cap-theorem", number=107, title="CAP Theorem",
        focus=("consistency","availability","partition tolerance","PACELC","quorum","split-brain"),
        overview="CAP theorem drives database selection in every system design interview. Know it deeply.",
        patterns=(), badge="HLD Core", tasks=(
        quiz("cap-1","What does CAP theorem actually state?","medium",("cap",),("During a network partition, you must choose between Consistency and Availability","A system can achieve Consistency, Availability, and Partition tolerance simultaneously","Relational databases cannot scale horizontally due to network latencies","During normal operations, a system must trade off performance for durability"),0,"During a partition (P), choose CP or AP. You can't have both C and A simultaneously."),
        quiz("cap-2","Is DynamoDB CP or AP?","hard",("cap",),("AP by default (eventually consistent reads), with optional strongly consistent reads","Strictly CP under all configurations since it uses a Paxos consensus group per partition","Neither, since managed cloud databases are exempt from the CAP theorem limits","AP only, as it cannot enforce strong consistency due to its multi-active replication design"),0,"DynamoDB defaults to eventually consistent (AP) but offers strongly consistent at higher cost."),
        quiz("cap-3","Is PostgreSQL CP or AP?","medium",("cap",),("CP — prioritizes consistency over availability during partitions","AP, because it prioritizes high availability and always accepts writes even on disconnected replicas","Neither, because the CAP theorem does not apply to single-instance databases","AP, because standard read replicas serve stale data during network partitions"),0,"Traditional RDBMS are CP: reject writes rather than risk inconsistency."),
        quiz("cap-4","What does PACELC add to CAP?","hard",("cap",),("When no partition (Else), you still choose between Latency and Consistency","States that a system must choose between Partition tolerance, Availability, and Latency","Proves that Consistency can be maintained during partitions if Latency is ignored","Formulates that availability is mathematically equivalent to the inverse of network latency"),0,"PACELC: if Partition → A or C. Else → L(atency) or C(onsistency). More nuanced."),
        quiz("cap-5","What is the quorum formula?","hard",("cap",),("W + R > N ensures overlap between write and read sets","W + R = N where writes and reads are balanced equally across all nodes","W = R = N/2 + 1 to ensure that all read and write operations require a strict majority","W + R < N to minimize write latency and maximize read throughput"),0,"For N=3: W=2, R=2 guarantees reading latest write. W+R=4 > 3."),
        quiz("cap-6","What is split-brain?","hard",("cap",),("When a partition causes two cluster halves to each believe they are leader, causing conflicting writes","A memory fragmentation issue where database servers allocate pages unevenly","A client-side load balancing bug that causes requests to loop between two servers","A network partition that forces a database instance to run multiple master processes"),0,"Split-brain can cause data corruption. Solutions: quorum leader election, fencing tokens, STONITH."),
        quiz("cap-7","What is a vector clock?","hard",("cap",),("A logical clock tracking causality across nodes using per-node counters","A physical atomic clock installed on servers to synchronize timestamps across regions","A load balancing metric that routes requests based on the rate of clock drift","A database index that orders transaction logs by their physical write time"),0,"Vector clocks detect causality and concurrency in distributed systems without synchronized clocks."),
        quiz("cap-8","What is Cassandra's consistency model?","hard",("cap",),("Tunable — choose consistency per query (ONE, QUORUM, ALL)","Strictly CP under all configurations due to its peer-to-peer ring architecture","Strictly AP, as it lacks the capability to perform strongly consistent read operations","Causal consistency only, as it tracks causal relationships using vector clocks"),0,"Cassandra lets you choose: ONE (fast, stale) to ALL (slow, consistent)."),
        quiz("cap-9","What is the two-generals problem?","hard",("cap",),("It's impossible to guarantee agreement between two parties over an unreliable network","A resource allocation deadlock where two processes compete for the same database locks","A CPU scheduling bottleneck when running two resource-intensive background workers","A routing conflict where two load balancers direct traffic to the same server"),0,"Proves consensus over unreliable networks is impossible — foundational to distributed systems."),
        quiz("cap-10","What is linearizability?","hard",("cap",),("The strongest consistency: operations appear to execute atomically at some point between invocation and response","A transaction isolation level that prevents dirty reads but allows non-repeatable reads","A database performance metric measuring query latency scalability under load","A consistency model where reads are guaranteed to return the state of the master replica"),0,"Linearizability provides real-time ordering guarantees. Expensive but sometimes necessary."),
        quiz("cap-11","What is causal consistency?","hard",("cap",),("Operations that are causally related are seen in the same order by all nodes","A model where operations are ordered chronologically using global physical clocks","A consistency model that guarantees all replicas return the same value simultaneously","An isolation level that guarantees serializable transactions across database shards"),0,"Weaker than linearizability but stronger than eventual. Preserves happens-before relationships."),
        quiz("cap-12","What is read-your-own-writes consistency?","medium",("cap",),("A user always sees their own most recent writes, even if other users see stale data","A transaction level that prevents dirty writes across concurrent user sessions","A database optimization where the engine caches the result of the user's last select query","A replication configuration where writes are synchronously committed to all read replicas"),0,"Achieved by routing reads to the leader or to a replica that has caught up with the user's last write."),
        quiz("cap-13","What is monotonic read consistency?","hard",("cap",),("Once you read a value, you never see an older value in subsequent reads","A performance metric indicating that read latency remains constant over time","A durability guarantee ensuring that once a record is written, it is never deleted","A consistency level where reads from any replica are guaranteed to return the absolute latest write"),0,"Without this, reading from different replicas could show time-traveling data."),
        quiz("cap-14","What is the Raft consensus algorithm?","hard",("cap",),("A leader-based consensus protocol: leader election, log replication, safety guarantees","A cryptographic hash function used to verify data integrity in distributed logs","A peer-to-peer routing protocol for load balancing across multi-region clusters","A data partitioning scheme that divides keys across nodes using a sorted ring"),0,"Raft is simpler than Paxos. Used by etcd, CockroachDB, TiKV for distributed consensus."),
        quiz("cap-15","What is a lease in distributed systems?","hard",("cap",),("A time-bounded lock that automatically expires, preventing indefinite lock holding","A connection pooling configuration that limits client session durations","An agreement between replicas to replicate data asynchronously","A database transaction isolation level that prevents dirty reads"),0,"Leases prevent zombie leaders from making changes after being replaced."),
        quiz("cap-16","What is a fencing token?","hard",("cap",),("A monotonically increasing token given with each lock grant to detect stale lock holders","A JSON Web Token used to authorize API requests at the gateway level","A random string used to prevent cross-site request forgery (CSRF) in forms","A unique ID generated by the client to deduplicate duplicate request submissions"),0,"Fencing tokens prevent split-brain: storage rejects writes with tokens lower than the latest seen."),
        quiz("cap-17","What is the difference between CP and AP systems in practice?","medium",("cap",),("CP systems (ZooKeeper, PostgreSQL) reject requests during partitions; AP systems (DynamoDB, Cassandra) serve stale data","CP systems are entirely centralized, whereas AP systems are entirely decentralized","AP systems guarantee absolute consistency at the expense of slower response times","CP systems use physical clocks for ordering, whereas AP systems use logical clocks"),0,"The choice depends on your application: banking needs CP; social feeds can tolerate AP."),
        quiz("cap-18","What is tunable consistency?","hard",("cap",),("Ability to choose consistency level per operation (e.g., Cassandra's ONE, QUORUM, ALL)","A network configuration that adjusts bandwidth allocations to control latency","A database scaling setting that increases the number of shards dynamically","An application-level rate limiter that limits write throughput during peak load"),0,"Tunable consistency lets you trade latency for freshness on a per-query basis."),
        quiz("cap-19","What is the gossip protocol?","hard",("cap",),("A peer-to-peer protocol where nodes periodically exchange state information","A synchronous replication mechanism where the leader blocks until all nodes acknowledge","A consensus algorithm like Paxos that guarantees absolute agreement on state changes","A query routing protocol that forwards client requests through intermediate hop nodes"),0,"Gossip propagates cluster state (membership, health) without a central coordinator. Eventually consistent."),
        quiz("cap-20","What is anti-entropy in distributed systems?","hard",("cap",),("A background process that compares data across replicas and repairs inconsistencies","A cryptographic algorithm that encrypts database logs to prevent tampering","A caching strategy that periodically evicts stale data from memory","A garbage collection process that reclaims storage from deleted database records"),0,"Anti-entropy (e.g., Merkle tree comparison) ensures replicas converge even after network issues."),
    ))

def numbers_lesson() -> Lesson:
    return Lesson(id="hld-numbers", number=108, title="Numbers to Know",
        focus=("latency","throughput","storage","back-of-envelope","Little's Law"),
        overview="The numbers let you estimate capacity, justify design choices, and show interviewers you think quantitatively.",
        patterns=(), badge="HLD Core", tasks=(
        quiz("num-1","What is the approximate latency of a same-datacenter round-trip?","medium",("numbers",),("0.5 ms","500 ms","50 ms","1 ns"),0,"Intra-DC: ~0.5ms. Cross-continent: ~100-150ms."),
        quiz("num-2","How many QPS can a single modern server handle for simple API requests?","medium",("numbers",),("10,000-50,000","Exactly 100 QPS","1 million QPS","Exactly 1,000 QPS"),0,"A well-optimized server handles 10K-50K simple API req/sec."),
        quiz("num-3","What is the approximate sequential read speed of an SSD?","medium",("numbers",),("500 MB/s - 3 GB/s","100 GB/s","1 MB/s","50 MB/s"),0,"Modern NVMe SSDs: 500MB/s to 3GB/s sequential. Random reads are slower."),
        quiz("num-4","How much data can a single PostgreSQL handle well?","hard",("numbers",),("A few hundred GB to low TB range","1 PB","1 GB","100 TB"),0,"PostgreSQL works well up to a few hundred GB to low terabytes."),
        quiz("num-5","1 billion users × 1 KB each = ?","easy",("numbers",),("1 TB","100 GB","1 GB","1 PB"),0,"1B × 1KB = 1TB. A common back-of-envelope calculation."),
        quiz("num-6","What is the approximate latency of reading 1 MB from memory?","hard",("numbers",),("250 μs (0.25 ms)","1 ns","10 ms","100 ms"),0,"Memory: ~250μs/MB. SSD: ~1ms. HDD: ~20ms."),
        quiz("num-7","If a service handles 10K req/s at 100ms each, how many concurrent connections?","medium",("numbers",),("1,000","100","100,000","10,000"),0,"Little's Law: Concurrency = Throughput × Latency = 10K × 0.1s = 1,000."),
        quiz("num-8","What is the cost ratio of RAM to SSD per GB?","medium",("numbers",),("RAM is ~30-50x more expensive","SSD is ~10x more expensive than RAM","They cost the same per gigabyte","RAM is exactly 2x more expensive than SSD"),0,"RAM: ~$5-10/GB. SSD: ~$0.10-0.25/GB. Drives caching and storage tier decisions."),
        quiz("num-9","How many bytes per second does a 1 Gbps link provide?","medium",("numbers",),("~125 MB/s","1 MB/s","1 GB/s","10 GB/s"),0,"1Gbps ÷ 8 = 125 MB/s. Real throughput slightly less due to overhead."),
        quiz("num-10","What does 99.999% (5 nines) availability mean in annual downtime?","medium",("numbers",),("~5 minutes/year","~52 minutes/year","~1 hour/year","~5 hours/year"),0,"99.999%=5.26min/yr. 99.9%=8.76hr/yr. 99.99%=52.6min/yr."),
        quiz("num-11","How many bytes in a UUID?","medium",("numbers",),("16","8","4","32"),0,"UUID = 128 bits = 16 bytes. As hex string with dashes: 36 characters."),
        quiz("num-12","What is the typical write throughput of a single HDD?","medium",("numbers",),("100 MB/s sequential, 1-2 MB/s random","1 GB/s sequential","10 GB/s sequential","1 MB/s sequential"),0,"HDDs: ~100MB/s sequential but only 100-200 IOPS random. SSDs: 100K+ IOPS."),
        quiz("num-13","How many daily active users can a single server handle?","medium",("numbers",),("1-10 million (depends on workload)","Max 10,000 users","Over 1 billion users","Max 100 users"),0,"Varies hugely by workload, but millions of DAU with proper architecture is feasible."),
        quiz("num-14","What is the typical latency of a DNS lookup?","medium",("numbers",),("1-100 ms","10 seconds","1 nanosecond","1 second"),0,"Cached DNS: ~1ms. Uncached with recursive resolution: 20-100ms."),
        quiz("num-15","How much storage does 1 million tweets (280 chars each) need?","medium",("numbers",),("280 MB","28 MB","28 GB","2.8 GB"),0,"1M × 280 bytes ≈ 280 MB. With metadata (user, timestamp, etc.): ~1-2GB."),
        quiz("num-16","What is the typical compression ratio for text data?","medium",("numbers",),("3:1 to 10:1 depending on data","100:1","1000:1","Exactly 1:1"),0,"gzip typically achieves 3-5x on text, 10x on repetitive data like logs."),
        quiz("num-17","What is Little's Law?","hard",("numbers",),("L = λ × W (average items = arrival rate × avg time in system)","A caching formula relating hit rate to cache size","A database theorem defining optimal index depth","An indexing rule for calculating B-tree node splits"),0,"Little's Law: concurrent requests = throughput × latency. Fundamental for capacity planning."),
        quiz("num-18","How many TCP connections can a single server maintain?","hard",("numbers",),("100K-1M with proper OS tuning","Strictly up to 10,000 connections","Up to 10 million connections without tuning","Strictly up to 1,000 connections"),0,"Each connection needs a file descriptor and some memory. With epoll and tuning, 1M+ is achievable."),
        quiz("num-19","What is the typical size of a database row in a social media app?","medium",("numbers",),("1 KB","1 byte","1 GB","1 MB"),0,"A typical row (user profile, post, etc.) is 0.5-2KB including all columns."),
        quiz("num-20","If you need 99.99% availability across 3 independent services in series, what availability does each need?","hard",("numbers",),("99.997% each (since 0.99997³ ≈ 0.9999)","Exactly 99.9%","Exactly 100%","Exactly 99.99%"),0,"Services in series multiply: for 99.99% overall, each service needs ~99.997% availability."),
    ))
