"""HLD Core Concepts: Sharding, Consistent Hashing, CAP Theorem, Numbers to Know — 20 MCQs each."""
from __future__ import annotations
from .lld_practice import quiz, Lesson

def sharding_lesson() -> Lesson:
    return Lesson(id="hld-sharding", number=105, title="Sharding",
        focus=("shard keys","range vs hash","hotspots","rebalancing","cross-shard queries"),
        overview="Sharding is how you scale databases horizontally. The shard key choice makes or breaks the design.",
        patterns=(), badge="HLD Core", tasks=(
        quiz("sh-1","What is database sharding?","easy",("sharding",),("Splitting a database across multiple machines, each holding a subset of data","A prefetching algorithm that predicts future access patterns from recent query history","A replication technique that mirrors hot cache entries across multiple nodes for availability","An eviction algorithm that considers both recency and frequency when selecting victims"),0,"Sharding distributes rows across nodes so no single machine handles all traffic."),
        quiz("sh-2","What makes a good shard key?","hard",("sharding",),("High cardinality, even distribution, frequently used in queries, rarely changing","A security protocol that authenticates both client and server during the handshake","A membership protocol where nodes periodically exchange state with random cluster peers","A data structure that stores cache metadata compactly using probabilistic counting"),0,"A good shard key distributes data evenly and supports common query patterns."),
        quiz("sh-3","What is a hot shard?","hard",("sharding",),("A deployment strategy that gradually shifts traffic from the old version to the new one","A sharding approach that distributes cached entries across nodes by consistent hashing","A shard receiving disproportionate traffic due to poor key distribution","A consistency model where reads from any replica are guaranteed to be monotonically fresh"),2,"Hot shards occur when a key value is too popular. Fix: composite keys, salting, range splitting."),
        quiz("sh-4","Range-based vs hash-based sharding?","medium",("sharding",),("A pattern where incoming requests are buffered and processed in priority-ordered batches","Range doesn't work with strings across multiple service instances","A clock synchronization technique that uses NTP with bounded drift for ordering events","Range assigns key ranges (good for scans); hash assigns by hash (good for distribution)"),3,"Range supports range queries but risks hotspots. Hash distributes evenly but makes range queries expensive."),
        quiz("sh-5","What is a cross-shard query?","hard",("sharding",),("A communication pattern where services exchange messages through a centralized broker","A query needing data from multiple shards, requiring scatter-gather and coordination","A monitoring tool that tracks hit rates, eviction counts, and memory usage per cache pool","A monitoring approach that aggregates metrics from multiple sources into unified dashboards"),1,"Cross-shard queries fan out to multiple nodes and require merging. Very expensive."),
        quiz("sh-6","What is shard rebalancing?","medium",("sharding",),("A scheduling algorithm that assigns tasks to workers based on estimated completion time","Moving data between shards when load becomes uneven or shards are added/removed","A technique where the server pre-fetches resources based on predicted client needs","A partitioning strategy that splits data based on a hash of the composite primary key"),1,"Rebalancing redistributes data — expensive, must be done carefully to avoid downtime."),
        quiz("sh-7","Why is auto-increment ID bad as a shard key?","hard",("sharding",),("A storage engine optimization that pre-sorts data on disk for sequential access patterns","Sequential IDs route all new writes to the same shard, creating a hotspot","A connection management approach that pins clients to specific backend instances","A testing approach that injects controlled failures to verify system recovery behavior"),1,"Sequential keys concentrate recent writes. Use UUIDs, Snowflake IDs, or hash-based assignment."),
        quiz("sh-8","What is directory-based sharding?","medium",("sharding",),("A warming strategy that pre-loads data based on access frequency from the previous day","A central service maps each key to its shard, allowing flexible placement at extra hop cost","A replication strategy where writes go to any node and conflicts are resolved on read","A cache coherence protocol that broadcasts invalidation messages to all cache tiers"),1,"Directory-based offers maximum flexibility but the directory becomes a SPOF."),
        quiz("sh-9","How does sharding affect JOINs?","hard",("sharding",),("A transport layer optimization that reduces round-trip overhead using multiplexing","JOINs across shards become very expensive or impossible — denormalize or co-locate","A process that periodically reconciles state between systems to detect and fix drift","A load shedding strategy that drops low-priority requests when resource utilization is high"),1,"Fundamental tradeoff: sharding sacrifices JOIN capability."),
        quiz("sh-10","What is geo-sharding?","medium",("sharding",),("An index maintenance operation that rebuilds fragmented pages for better scan performance","Partitioning data by geographic region so users' data is stored near them","A state synchronization approach that reconciles differences using Merkle tree comparison","A flow control mechanism that prevents the sender from overwhelming the receiver"),1,"Geo-sharding reduces latency and helps with data residency regulations (GDPR)."),
        quiz("sh-11","What is scatter-gather?","hard",("sharding",),("A partitioning algorithm that assigns data based on a hash of the geographic region","Send query to all shards (scatter), collect results, merge and return (gather)","A transaction isolation level that prevents phantom reads using predicate locking","A DNS-based technique that distributes queries across multiple authoritative servers"),1,"Scatter-gather is necessary when the shard key isn't in the query."),
        quiz("sh-12","Sharding vs partitioning — what's the difference?","medium",("sharding",),("Partitioning is general concept; sharding distributes partitions across different machines","A serialization format that reduces cache entry size through schema-aware compression","Partitioning is for reads only with automatic failover support","A monitoring tool that tracks query execution plans and identifies slow running queries"),0,"A single PostgreSQL can have partitions (one machine). Sharding distributes across nodes."),
        quiz("sh-13","How does Instagram generate sharded IDs?","hard",("sharding",),("Snowflake-like: timestamp + shard ID + sequence number — unique, sortable, shard-aware","A persistence mechanism that snapshots the cache state to disk for crash recovery","A failure detection mechanism using heartbeat intervals and configurable timeout thresholds","A replication technique that synchronously mirrors writes to a standby for zero data loss"),0,"41 bits timestamp + 13 bits shard ID + 10 bits sequence = globally unique, chronologically sortable."),
        quiz("sh-14","What is data co-location in sharding?","medium",("sharding",),("Placing related data on the same shard to enable efficient local JOINs and transactions","A consensus protocol that uses a rotating leader to distribute write coordination load","A resilience pattern that isolates failures in one subsystem from propagating to others","Storing all on one server using configurable thresholds"),0,"Co-location avoids cross-shard operations. Example: shard by user_id for all user data."),
        quiz("sh-15","What happens to transactions in a sharded DB?","hard",("sharding",),("Single-shard transactions work; cross-shard require 2PC or Saga patterns","A connection management approach that limits concurrent sessions to prevent overload","A backup mechanism that captures incremental changes since the last full snapshot","A coordination service that uses ephemeral nodes for automatic failure detection"),0,"Cross-shard transactions are expensive (2PC) or must be redesigned as eventual consistency workflows."),
        quiz("sh-16","What is resharding?","hard",("sharding",),("A congestion avoidance algorithm that dynamically adjusts the transmission window","A routing strategy that directs traffic based on geographic proximity of the client","Changing the sharding scheme, requiring massive data migration with minimal downtime","A query optimization strategy that rewrites subqueries into more efficient join patterns"),2,"Resharding is painful. Plan your shard key carefully upfront to avoid it."),
        quiz("sh-17","For a social network, why shard by user_id?","hard",("sharding",),("Most queries are user-centric (my feed, posts) so co-locating user data avoids cross-shard queries","A protocol that batches multiple requests into a single TCP segment for efficiency","A mechanism that compresses HTTP headers to reduce bandwidth on repeated requests","A schema design pattern that embeds frequently accessed data to avoid join overhead"),0,"User-centric access patterns dominate social networks."),
        quiz("sh-18","What is the challenge of unique constraints across shards?","hard",("sharding",),("A data pipeline stage that transforms and enriches events before writing to storage","Unique constraints are auto using configurable thresholds","Each shard only sees its data; global uniqueness requires centralized check or clever key design","A technique that partitions workload across worker pools based on resource requirements"),2,"Options: Snowflake IDs, centralized uniqueness service, or inherently unique key design."),
        quiz("sh-19","What is virtual sharding?","hard",("sharding",),("Sharding without DB — commonly used in production environments","Each physical node owns multiple virtual positions on the hash ring for better load distribution","Cloud sharding — commonly used in production environments","A simulation — commonly used in production environments"),1,"Virtual nodes improve balance in consistent hashing by giving each node multiple ring positions."),
        quiz("sh-20","What is a shard proxy?","medium",("sharding",),("A routing layer that directs queries to the correct shard based on the shard key","A caching layer — commonly used in production environments","A load balancer — commonly used in production environments","A database index — commonly used in production environments"),0,"Shard proxies (e.g., Vitess for MySQL) handle routing, connection pooling, and query rewriting."),
    ))

def consistent_hashing_lesson() -> Lesson:
    return Lesson(id="hld-consistent-hashing", number=106, title="Consistent Hashing",
        focus=("hash ring","virtual nodes","rebalancing","load distribution","key mapping"),
        overview="Consistent hashing minimizes key redistribution when nodes are added/removed. Essential for distributed caches and databases.",
        patterns=(), badge="HLD Core", tasks=(
        quiz("ch-1","What problem does consistent hashing solve?","medium",("consistent-hashing",),("Compression — commonly used in production environments","Sorting data — commonly used in production environments","Encryption — commonly used in production environments","When nodes are added/removed, only a small fraction of keys need remapping (vs modulo remapping all)"),3,"With modulo hashing, adding a node remaps almost all keys. Consistent hashing remaps only ~1/N."),
        quiz("ch-2","How does a consistent hash ring work?","medium",("consistent-hashing",),("A linked list — commonly used in production environments","A tree structure — commonly used in production environments","A hash table — commonly used in production environments","Nodes and keys are hashed onto a circular ring; each key is assigned to the first node clockwise from it"),3,"The ring is [0, 2^32). Hash both node IPs and keys. Walk clockwise from key's position to find its node."),
        quiz("ch-3","What are virtual nodes (vnodes)?","hard",("consistent-hashing",),("Cloud VMs — commonly used in production environments","Each physical node maps to multiple positions on the hash ring for better load distribution","Simulated nodes — commonly used in production environments","Virtual machines — commonly used in production environments"),1,"Without vnodes, nodes with different capacities or unlucky hash positions get uneven load."),
        quiz("ch-4","How many virtual nodes per physical node is typical?","hard",("consistent-hashing",),("10 — commonly used in production environments","1 — commonly used in production environments","100-200 vnodes per physical node for good distribution","1000 — commonly used in production environments"),2,"More vnodes = smoother distribution. Cassandra defaults to 256 tokens per node."),
        quiz("ch-5","What happens when a node is added to the ring?","medium",("consistent-hashing",),("Only keys between the new node and its predecessor are moved to the new node","Nothing changes — commonly used in production environments","All keys remapped — commonly used in production environments","All nodes rebalance — commonly used in production environments"),0,"Only ~1/N keys move on average. This is the key advantage over modulo hashing."),
        quiz("ch-6","What happens when a node is removed?","medium",("consistent-hashing",),("Ring collapses — commonly used in production environments","All keys remapped — commonly used in production environments","Its keys are reassigned to the next node clockwise on the ring","All data lost — commonly used in production environments"),2,"Only the failed node's keys move. All other assignments remain stable."),
        quiz("ch-7","Why does modulo hashing (key % N) fail when N changes?","medium",("consistent-hashing",),("It doesn't fail — commonly used in production environments","Changing N (nodes) remaps almost every key to a different node, causing massive cache misses","Modulo is slower — commonly used in production environments","Modulo doesn't distribute evenly across multiple service instances"),1,"Going from 3→4 nodes: 2/3 of keys remap. Consistent hashing: only 1/4."),
        quiz("ch-8","Which systems use consistent hashing?","medium",("consistent-hashing",),("Only caches — commonly used in production environments","Only load balancers — commonly used in production environments","DynamoDB, Cassandra, Memcached, CDNs, and distributed caches","Only databases — commonly used in production environments"),2,"It's fundamental to any distributed system that partitions data across nodes."),
        quiz("ch-9","How does consistent hashing relate to replication?","hard",("consistent-hashing",),("Keys can be replicated to the next N nodes clockwise on the ring for fault tolerance","Replication replaces hashing in distributed environments","Hashing prevents replication across multiple service instances","It doesn't — commonly used in production environments"),0,"DynamoDB replicates each key to 3 nodes clockwise on the ring (preferring distinct physical nodes)."),
        quiz("ch-10","What is the load balancing challenge with consistent hashing?","hard",("consistent-hashing",),("It's too slow — commonly used in production environments","Without virtual nodes, nodes can get very uneven loads due to random hash distribution","No challenge — commonly used in production environments","It uses too much memory — commonly used in production environments"),1,"With K physical nodes and no vnodes, standard deviation of load is O(1/√K) — too high for small K."),
        quiz("ch-11","How does DynamoDB use consistent hashing?","hard",("consistent-hashing",),("Uses modulo hashing — commonly used in production environments","Partitions data across storage nodes with virtual nodes and preference lists for replication","It doesn't — commonly used in production environments","Uses range sharding only — commonly used in production environments"),1,"DynamoDB's consistent hashing with vnodes and coordinator-based routing. Described in the Dynamo paper."),
        quiz("ch-12","What is a preference list in DynamoDB's consistent hashing?","hard",("consistent-hashing",),("A query optimization — commonly used in production environments","The ordered list of nodes responsible for storing replicas of a key, skipping same-rack nodes","A caching strategy — commonly used in production environments","A user preference — commonly used in production environments"),1,"The preference list ensures replicas land on distinct physical nodes for fault tolerance."),
        quiz("ch-13","How does consistent hashing handle heterogeneous nodes?","hard",("consistent-hashing",),("It can't — commonly used in production environments","Assign more virtual nodes to higher-capacity machines so they handle proportionally more keys","Use same-size nodes only — commonly used in production environments","Use separate rings — commonly used in production environments"),1,"A 2x capacity node gets 2x vnodes, receiving roughly 2x the traffic."),
        quiz("ch-14","What is jump consistent hash?","hard",("consistent-hashing",),("A ring-based hash — commonly used in production environments","A binary search — commonly used in production environments","A modulo variant — commonly used in production environments","A hash algorithm that maps keys to buckets uniformly with O(ln n) time and no ring overhead"),3,"Jump hash is simpler and more uniform than ring-based consistent hashing, but only supports sequential bucket IDs."),
        quiz("ch-15","What is rendezvous (highest random weight) hashing?","hard",("consistent-hashing",),("A caching strategy — commonly used in production environments","Each key computes a score for each node; the highest-scoring node wins — no ring needed","A tree structure — commonly used in production environments","A load balancing algorithm with automatic failover support"),1,"Rendezvous hashing: simpler conceptually but O(N) per lookup. Good for small N."),
        quiz("ch-16","How does Cassandra use consistent hashing?","hard",("consistent-hashing",),("Each node owns token ranges on the ring; data is placed based on partition key hash","Uses range-based only — commonly used in production environments","It doesn't — commonly used in production environments","Uses modulo hashing — commonly used in production environments"),0,"Cassandra uses Murmur3 hash of the partition key to place data on the ring."),
        quiz("ch-17","What is the impact of consistent hashing on cache hit rates during scaling?","hard",("consistent-hashing",),("No impact — commonly used in production environments","Adding a node causes only ~1/N cache misses instead of near-total cache invalidation","50% cache miss — commonly used in production environments","100% cache miss — commonly used in production environments"),1,"This is why CDNs and distributed caches use consistent hashing — scaling doesn't destroy cache."),
        quiz("ch-18","How does consistent hashing handle rack/zone awareness?","hard",("consistent-hashing",),("It doesn't — commonly used in production environments","Racks are irrelevant — commonly used in production environments","Skip vnodes in the same rack when building replica sets to ensure fault tolerance across failure domains","Only one rack is used — commonly used in production environments"),2,"Network topology-aware placement ensures replicas survive rack/zone failures."),
        quiz("ch-19","What is bounded load consistent hashing?","hard",("consistent-hashing",),("Load limiting — commonly used in production environments","A rate limiter — commonly used in production environments","Variant where each node has a max load cap — if a node is overloaded, keys spill to the next node","A firewall rule — commonly used in production environments"),2,"Google's bounded load variant prevents hotspots by redistributing overflow keys."),
        quiz("ch-20","What is the relationship between consistent hashing and DHTs?","hard",("consistent-hashing",),("None — commonly used in production environments","DHTs replace hashing — commonly used in production environments","Hashing replaces DHTs — commonly used in production environments","DHTs (Chord, Kademlia) use consistent hashing as the foundation for decentralized key-value lookup"),3,"DHTs extend consistent hashing with routing tables for O(log N) lookups in peer-to-peer systems."),
    ))

def cap_theorem_lesson() -> Lesson:
    return Lesson(id="hld-cap-theorem", number=107, title="CAP Theorem",
        focus=("consistency","availability","partition tolerance","PACELC","quorum","split-brain"),
        overview="CAP theorem drives database selection in every system design interview. Know it deeply.",
        patterns=(), badge="HLD Core", tasks=(
        quiz("cap-1","What does CAP theorem actually state?","medium",("cap",),("CPU, Availability, Performance with automatic failover support","During a network partition, you must choose between Consistency and Availability","Only applies to SQL — commonly used in production environments","Pick all three — commonly used in production environments"),1,"During a partition (P), choose CP or AP. You can't have both C and A simultaneously."),
        quiz("cap-2","Is DynamoDB CP or AP?","hard",("cap",),("CP — commonly used in production environments","Neither — commonly used in production environments","AP by default (eventually consistent reads), with optional strongly consistent reads","Both — commonly used in production environments"),2,"DynamoDB defaults to eventually consistent (AP) but offers strongly consistent at higher cost."),
        quiz("cap-3","Is PostgreSQL CP or AP?","medium",("cap",),("Depends on version — commonly used in production environments","Neither — commonly used in production environments","CP — prioritizes consistency over availability during partitions","AP — commonly used in production environments"),2,"Traditional RDBMS are CP: reject writes rather than risk inconsistency."),
        quiz("cap-4","What does PACELC add to CAP?","hard",("cap",),("Adds Performance — commonly used in production environments","Replaces CAP — commonly used in production environments","Nothing — commonly used in production environments","When no partition (Else), you still choose between Latency and Consistency"),3,"PACELC: if Partition → A or C. Else → L(atency) or C(onsistency). More nuanced."),
        quiz("cap-5","What is the quorum formula?","hard",("cap",),("N/2 — commonly used in production environments","N - 1 — commonly used in production environments","W + R > N ensures overlap between write and read sets","Always N — commonly used in production environments"),2,"For N=3: W=2, R=2 guarantees reading latest write. W+R=4 > 3."),
        quiz("cap-6","What is split-brain?","hard",("cap",),("When a partition causes two cluster halves to each believe they are leader, causing conflicting writes","A caching issue — commonly used in production environments","A LB failure — commonly used in production environments","A database error — commonly used in production environments"),0,"Split-brain can cause data corruption. Solutions: quorum leader election, fencing tokens, STONITH."),
        quiz("cap-7","What is a vector clock?","hard",("cap",),("A LB algorithm — commonly used in production environments","A logical clock tracking causality across nodes using per-node counters","A timestamp format — commonly used in production environments","A physical clock — commonly used in production environments"),1,"Vector clocks detect causality and concurrency in distributed systems without synchronized clocks."),
        quiz("cap-8","What is Cassandra's consistency model?","hard",("cap",),("Tunable — choose consistency per query (ONE, QUORUM, ALL)","Always strong — commonly used in production environments","Same as PostgreSQL — commonly used in production environments","Eventually consistent only using configurable thresholds"),0,"Cassandra lets you choose: ONE (fast, stale) to ALL (slow, consistent)."),
        quiz("cap-9","What is the two-generals problem?","hard",("cap",),("A LB issue — commonly used in production environments","A caching problem — commonly used in production environments","A scheduling problem — commonly used in production environments","It's impossible to guarantee agreement between two parties over an unreliable network"),3,"Proves consensus over unreliable networks is impossible — foundational to distributed systems."),
        quiz("cap-10","What is linearizability?","hard",("cap",),("A weak consistency — commonly used in production environments","The strongest consistency: operations appear to execute atomically at some point between invocation and response","Only for SQL — commonly used in production environments","Same as serializability — commonly used in production environments"),1,"Linearizability provides real-time ordering guarantees. Expensive but sometimes necessary."),
        quiz("cap-11","What is causal consistency?","hard",("cap",),("Same as eventual — commonly used in production environments","The strongest model — commonly used in production environments","Only for NoSQL — commonly used in production environments","Operations that are causally related are seen in the same order by all nodes"),3,"Weaker than linearizability but stronger than eventual. Preserves happens-before relationships."),
        quiz("cap-12","What is read-your-own-writes consistency?","medium",("cap",),("A caching strategy — commonly used in production environments","A user always sees their own most recent writes, even if other users see stale data","Always consistent — commonly used in production environments","A replication mode — commonly used in production environments"),1,"Achieved by routing reads to the leader or to a replica that has caught up with the user's last write."),
        quiz("cap-13","What is monotonic read consistency?","hard",("cap",),("Once you read a value, you never see an older value in subsequent reads","Reads get faster — commonly used in production environments","A write guarantee — commonly used in production environments","Reads are ordered — commonly used in production environments"),0,"Without this, reading from different replicas could show time-traveling data."),
        quiz("cap-14","What is the Raft consensus algorithm?","hard",("cap",),("A leader-based consensus protocol: leader election, log replication, safety guarantees","A hash algorithm — commonly used in production environments","A load balancer — commonly used in production environments","A sharding strategy — commonly used in production environments"),0,"Raft is simpler than Paxos. Used by etcd, CockroachDB, TiKV for distributed consensus."),
        quiz("cap-15","What is a lease in distributed systems?","hard",("cap",),("A connection — commonly used in production environments","A rental agreement — commonly used in production environments","A session — commonly used in production environments","A time-bounded lock that automatically expires, preventing indefinite lock holding"),3,"Leases prevent zombie leaders from making changes after being replaced."),
        quiz("cap-16","What is a fencing token?","hard",("cap",),("A JWT — commonly used in production environments","A monotonically increasing token given with each lock grant to detect stale lock holders","An API key — commonly used in production environments","A security token — commonly used in production environments"),1,"Fencing tokens prevent split-brain: storage rejects writes with tokens lower than the latest seen."),
        quiz("cap-17","What is the difference between CP and AP systems in practice?","medium",("cap",),("Only theoretical — commonly used in production environments","AP is always better — commonly used in production environments","CP is always faster — commonly used in production environments","CP systems (ZooKeeper, PostgreSQL) reject requests during partitions; AP systems (DynamoDB, Cassandra) serve stale data"),3,"The choice depends on your application: banking needs CP; social feeds can tolerate AP."),
        quiz("cap-18","What is tunable consistency?","hard",("cap",),("Fixed consistency — commonly used in production environments","An API setting — commonly used in production environments","A database feature — commonly used in production environments","Ability to choose consistency level per operation (e.g., Cassandra's ONE, QUORUM, ALL)"),3,"Tunable consistency lets you trade latency for freshness on a per-query basis."),
        quiz("cap-19","What is the gossip protocol?","hard",("cap",),("A replication mechanism — commonly used in production environments","A consensus algorithm — commonly used in production environments","A peer-to-peer protocol where nodes periodically exchange state information","A query protocol — commonly used in production environments"),2,"Gossip propagates cluster state (membership, health) without a central coordinator. Eventually consistent."),
        quiz("cap-20","What is anti-entropy in distributed systems?","hard",("cap",),("A background process that compares data across replicas and repairs inconsistencies","A caching strategy — commonly used in production environments","Data corruption — commonly used in production environments","An encryption technique — commonly used in production environments"),0,"Anti-entropy (e.g., Merkle tree comparison) ensures replicas converge even after network issues."),
    ))

def numbers_lesson() -> Lesson:
    return Lesson(id="hld-numbers", number=108, title="Numbers to Know",
        focus=("latency","throughput","storage","back-of-envelope","Little's Law"),
        overview="The numbers let you estimate capacity, justify design choices, and show interviewers you think quantitatively.",
        patterns=(), badge="HLD Core", tasks=(
        quiz("num-1","What is the approximate latency of a same-datacenter round-trip?","medium",("numbers",),("500 ms","50 ms","1 ns","0.5 ms"),3,"Intra-DC: ~0.5ms. Cross-continent: ~100-150ms."),
        quiz("num-2","How many QPS can a single modern server handle for simple API requests?","medium",("numbers",),("10,000-50,000","100 — commonly used in production environments","1 million","1,000 — commonly used in production environments"),0,"A well-optimized server handles 10K-50K simple API req/sec."),
        quiz("num-3","What is the approximate sequential read speed of an SSD?","medium",("numbers",),("100 GB/s — commonly used in production environments","500 MB/s - 3 GB/s","1 MB/s — commonly used in production environments","50 MB/s — commonly used in production environments"),1,"Modern NVMe SSDs: 500MB/s to 3GB/s sequential. Random reads are slower."),
        quiz("num-4","How much data can a single PostgreSQL handle well?","hard",("numbers",),("1 PB — commonly used in production environments","A few hundred GB to low TB range","1 GB — commonly used in production environments","100 TB — commonly used in production environments"),1,"PostgreSQL works well up to a few hundred GB to low terabytes."),
        quiz("num-5","1 billion users × 1 KB each = ?","easy",("numbers",),("100 GB","1 GB","1 TB","1 PB"),2,"1B × 1KB = 1TB. A common back-of-envelope calculation."),
        quiz("num-6","What is the approximate latency of reading 1 MB from memory?","hard",("numbers",),("1 ns — commonly used in production environments","250 μs (0.25 ms)","10 ms — commonly used in production environments","100 ms — commonly used in production environments"),1,"Memory: ~250μs/MB. SSD: ~1ms. HDD: ~20ms."),
        quiz("num-7","If a service handles 10K req/s at 100ms each, how many concurrent connections?","medium",("numbers",),("100","1,000","100,000","10,000"),1,"Little's Law: Concurrency = Throughput × Latency = 10K × 0.1s = 1,000."),
        quiz("num-8","What is the cost ratio of RAM to SSD per GB?","medium",("numbers",),("RAM is ~30-50x more expensive","SSD is more expensive","Same cost","RAM is 2x more"),0,"RAM: ~$5-10/GB. SSD: ~$0.10-0.25/GB. Drives caching and storage tier decisions."),
        quiz("num-9","How many bytes per second does a 1 Gbps link provide?","medium",("numbers",),("1 MB/s","1 GB/s","~125 MB/s","10 GB/s"),2,"1Gbps ÷ 8 = 125 MB/s. Real throughput slightly less due to overhead."),
        quiz("num-10","What does 99.999% (5 nines) availability mean in annual downtime?","medium",("numbers",),("~52 minutes/year","~1 hour/year","~5 minutes/year","~5 hours/year"),2,"99.999%=5.26min/yr. 99.99%=52.6min/yr. 99.9%=8.76hr/yr."),
        quiz("num-11","How many bytes in a UUID?","medium",("numbers",),("8","4","32","16"),3,"UUID = 128 bits = 16 bytes. As hex string with dashes: 36 characters."),
        quiz("num-12","What is the typical write throughput of a single HDD?","medium",("numbers",),("1 GB/s — commonly used in production environments","10 GB/s — commonly used in production environments","1 MB/s — commonly used in production environments","100 MB/s sequential, 1-2 MB/s random"),3,"HDDs: ~100MB/s sequential but only 100-200 IOPS random. SSDs: 100K+ IOPS."),
        quiz("num-13","How many daily active users can a single server handle?","medium",("numbers",),("10,000 — commonly used in production environments","1-10 million (depends on workload)","1 billion — commonly used in production environments","100 — commonly used in production environments"),1,"Varies hugely by workload, but millions of DAU with proper architecture is feasible."),
        quiz("num-14","What is the typical latency of a DNS lookup?","medium",("numbers",),("10 seconds","1-100 ms","1 ns","1 second"),1,"Cached DNS: ~1ms. Uncached with recursive resolution: 20-100ms."),
        quiz("num-15","How much storage does 1 million tweets (280 chars each) need?","medium",("numbers",),("28 MB","280 MB","28 GB","2.8 GB"),1,"1M × 280 bytes ≈ 280 MB. With metadata (user, timestamp, etc.): ~1-2GB."),
        quiz("num-16","What is the typical compression ratio for text data?","medium",("numbers",),("3:1 to 10:1 depending on data","100:1 — commonly used in production environments","1000:1 — commonly used in production environments","1:1 (no compression)"),0,"gzip typically achieves 3-5x on text, 10x on repetitive data like logs."),
        quiz("num-17","What is Little's Law?","hard",("numbers",),("L = λ × W (average items = arrival rate × avg time in system)","A caching formula — commonly used in production environments","A database rule — commonly used in production environments","An indexing theorem — commonly used in production environments"),0,"Little's Law: concurrent requests = throughput × latency. Fundamental for capacity planning."),
        quiz("num-18","How many TCP connections can a single server maintain?","hard",("numbers",),("10,000 — commonly used in production environments","10 million — commonly used in production environments","1,000 — commonly used in production environments","100K-1M with proper OS tuning"),3,"Each connection needs a file descriptor and some memory. With epoll and tuning, 1M+ is achievable."),
        quiz("num-19","What is the typical size of a database row in a social media app?","medium",("numbers",),("1 KB","1 byte","1 GB","1 MB"),0,"A typical row (user profile, post, etc.) is 0.5-2KB including all columns."),
        quiz("num-20","If you need 99.99% availability across 3 independent services in series, what availability does each need?","hard",("numbers",),("99.9% — commonly used in production environments","100% — commonly used in production environments","99.99% — commonly used in production environments","99.997% each (since 0.99997³ ≈ 0.9999)"),3,"Services in series multiply: for 99.99% overall, each service needs ~99.997% availability."),
    ))
